---
phase: 10-server-authoritative-combat-and-tracker
plan: 04
type: execute
wave: 3
depends_on: [10-03]
files_modified:
  - src/app/api/sessions/[id]/turn/route.ts
  - tests/app/api/sessions/[id]/turn/empty-narration-notify.test.ts
autonomous: true
requirements: [REQ-046]
must_haves:
  truths:
    - "When a server-resolved combat turn produces empty narration (combatStateChanged true), the route emits notifySession(sessionId, {type:'state'}) INSTEAD OF the existing {type:'turn-error', reason:'empty_response'} notify, so the client refetches (tracker updates) without showing a spurious 'no response / retry' error toast on a turn that legitimately advanced state"
    - "The combatStateChanged = _resolver !== null || _monsterLoopRan || openerRan guard branches the empty-narration else: combat → state notify; genuine non-combat empty → the original turn-error notify still fires unchanged"
    - "The {type:'state'} event the client already maps to refetch() (use-session-stream.ts:113-116) is the refresh mechanism; the {type:'turn-error'} event the client maps to refetch() + an error toast (use-session-stream.ts:120-134) is preserved ONLY for the genuine non-combat empty case"
  artifacts:
    - path: "src/app/api/sessions/[id]/turn/route.ts"
      provides: "Branched empty-narration else: combatStateChanged → notifySession({type:'state'}); else the pre-existing notifySession({type:'turn-error', reason:'empty_response'})"
      contains: "combatStateChanged"
    - path: "tests/app/api/sessions/[id]/turn/empty-narration-notify.test.ts"
      provides: "Test of the branch decision: combat empty turn → state notify fired AND turn-error NOT fired; non-combat empty turn → turn-error still fired AND state NOT fired"
      contains: "type: 'state'"
  key_links:
    - from: "src/app/api/sessions/[id]/turn/route.ts (empty-narration else branch ~748-755)"
      to: "src/sessions/notify.ts notifySession(sessionId, {type:'state'})"
      via: "guarded emit when combatStateChanged (replacing the turn-error emit on that branch)"
      pattern: "notifySession.*state"
    - from: "src/sessions/notify.ts {type:'state'} event"
      to: "src/sessions/use-session-stream.ts:113-116 → refetch()"
      via: "EventSource state handler the client already maps to refetch"
      pattern: "refetch"
---

<objective>
Close the residual REQ-046 tracker-refresh gap for the EMPTY-NARRATION case WITHOUT introducing a UX regression. The client-side snapshot recovery is ALREADY SHIPPED: game-client.tsx fires `void refetch()` at ~238 (finalizedSeq effect) and ~297 (safety poll), and use-session-stream.ts:113-116 already maps a `{type:'state'}` SSE event to `refetch()`.

CRITICAL CORRECTION (was stale in the prior draft): route.ts's empty-narration `else` branch (~748-755) ALREADY emits `notifySession(sessionId, {type:'turn-error', reason:'empty_response'})`, and the client maps `turn-error` to BOTH a `refetch()` AND an error toast ("il Master non ha prodotto risposta" / retry) at use-session-stream.ts:120-134. So the tracker is NOT entirely stuck today — but on a server-authoritative SILENT COMBAT turn (HP/initiative legitimately advanced, narration empty) the client shows a spurious "no response / retry" error even though the turn succeeded. Blindly ADDING a second `{type:'state'}` notify would fire a refresh AND that bogus error toast together — a UX regression.

The load-bearing REQUIRED fix is Option S, BRANCHED on combatStateChanged: in the empty-narration `else` branch, compute `combatStateChanged = _resolver !== null || _monsterLoopRan || openerRan`. When TRUE (a real server-resolved combat turn that legitimately advanced state), emit `notifySession(sessionId, {type:'state'})` INSTEAD OF the `turn-error` notify — the client refetches (tracker updates) and shows no error. When FALSE (a genuine non-combat empty turn — an actual model failure), keep the EXISTING `{type:'turn-error', reason:'empty_response'}` emit unchanged.

Option C is explicitly DOWNGRADED to a verification step: confirm the already-present `void refetch()` at game-client.tsx:238/297 covers the dropped-SSE / durable path; extend ONLY if a concrete gap is found — do NOT plan a from-scratch client change to a file that already recovers.

Purpose: A server-authoritative combat change with no narration refreshes the tracker AND does not show a bogus failure toast; genuine non-combat empties still surface the existing retry error.
Output: one branched server-side notify + a focused test; a verification note on the already-shipped client recovery.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-server-authoritative-combat-and-tracker/10-CONTEXT.md
@.planning/phases/10-server-authoritative-combat-and-tracker/10-RESEARCH.md
@src/app/api/sessions/[id]/turn/route.ts
@src/sessions/notify.ts
@src/sessions/use-session-stream.ts
@src/app/(authed)/sessions/[id]/game-client.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Branch the empty-narration else — combatStateChanged → {type:'state'} INSTEAD OF the existing turn-error notify</name>
  <files>src/app/api/sessions/[id]/turn/route.ts</files>
  <read_first>
    - src/app/api/sessions/[id]/turn/route.ts — CONFIRMED anchors: the empty-narration `else` branch ~748-755. THIS BRANCH ALREADY CALLS notifySession(sessionId, {type:'turn-error', reason:'empty_response'}) at ~750-754 (do not duplicate it — you are BRANCHING it). `_resolver` declared ~337, `_monsterLoopRan` declared ~399, and `openerRan` declared at the vault-branch scope by 10-03 (a boolean). All three are in scope at ~748. notifySession is therefore already imported in this file (it is the call you are branching).
    - src/sessions/notify.ts — CONFIRMED notifySession(sessionId, payload). payload.type may be 'state' (the silent refresh) or 'turn-error' (carries reason). The {type:'state'} payload carries no extra fields.
    - src/sessions/use-session-stream.ts — CONFIRMED 113-116: the EventSource handler maps {type:'state'} → refetch() (silent, no toast). CONFIRMED 120-134: it maps {type:'turn-error'} → refetch() AND an error toast (the "no response / retry" UX). This is exactly why a combat turn that advanced state must emit 'state', not 'turn-error'. READ ONLY — do not edit this file.
    - .planning/phases/10-server-authoritative-combat-and-tracker/10-CONTEXT.md — REQ-046 tracker-refresh decision; v1/v2 stack untouched (this plan only re-routes the existing notify in the else branch; it does not edit combat-resolver/monster-turns/projector math).
  </read_first>
  <action>
    In route.ts's empty-narration `else` branch (~748-755), do NOT add a second notify alongside the existing turn-error emit — BRANCH the existing emit:
    1. Compute `const combatStateChanged = _resolver !== null || _monsterLoopRan || openerRan;` (read the three existing in-scope signals — _resolver from ~337, _monsterLoopRan from ~399, openerRan the boolean 10-03 declared at the vault-branch scope).
    2. If `combatStateChanged` is TRUE: call `notifySession(sessionId, { type: 'state' })` (silent refresh — the client refetches and the tracker updates with NO error toast) and DO NOT emit the {type:'turn-error'} notify on this path. This is a real server-resolved combat turn that legitimately advanced state; it is not an error.
    3. If `combatStateChanged` is FALSE: keep the EXISTING `notifySession(sessionId, { type: 'turn-error', reason: 'empty_response' })` emit exactly as it is today (a genuine non-combat empty turn = an actual model failure that should still surface the retry toast).
    Structure it as an if/else on combatStateChanged so EXACTLY ONE notify fires per empty turn (state XOR turn-error) — never both. Do NOT touch use-session-stream.ts or game-client.tsx (client recovery is already shipped, and the client mappings for both event types already exist). Do NOT edit any v1/v2 file. The only change is re-routing the already-present notify in the else branch based on combatStateChanged.
  </action>
  <verify>
    <automated>grep -nE "notifySession\([^)]*type:[[:space:]]*'state'" "src/app/api/sessions/[id]/turn/route.ts" | grep -v '^[0-9]*:[[:space:]]*//' | grep -qE "." && grep -qE "combatStateChanged[[:space:]]*=[^;]*_resolver[^;]*_monsterLoopRan[^;]*openerRan" "src/app/api/sessions/[id]/turn/route.ts" && echo NOTIFY_OK</automated>
  </verify>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -qiE "turn/route" && echo TS_HAS_ERRORS || echo TS_CLEAN</automated>
  </verify>
  <done>The empty-narration else branch computes `combatStateChanged = _resolver !== null || _monsterLoopRan || openerRan` and branches the EXISTING notify: when true it emits notifySession(sessionId, {type:'state'}) and NOT the turn-error notify; when false it keeps the original {type:'turn-error', reason:'empty_response'} emit. Exactly one notify fires per empty turn. use-session-stream.ts and game-client.tsx are unmodified; tsc is clean; no v1/v2 file edited.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test the branch decision (combat empty → state, NOT turn-error; non-combat empty → turn-error, NOT state) + verify client recovery already present</name>
  <files>tests/app/api/sessions/[id]/turn/empty-narration-notify.test.ts</files>
  <read_first>
    - src/app/api/sessions/[id]/turn/route.ts — the empty-narration else branch (~748-755) AFTER Task 1's edit: the if/else on `combatStateChanged = _resolver !== null || _monsterLoopRan || openerRan`. NOTE: route.ts has NO unit-test harness (no exported handler fixture; the turn/ tests under tests/app/api/sessions/[id]/turn/ are pure-function tests — combat-resolver.test.ts, monster-bestiary*.test.ts — not full route drives), and a full headless route drive is NOT cheap. Therefore this test asserts the BRANCH DECISION at the smallest correct unit (option b), NOT a full route invocation.
    - tests/app/api/sessions/[id]/turn/combat-resolver.test.ts — the turn/ pure-function test conventions (direct import, deterministic, no live DB, no HTTP server) to mirror.
    - src/app/(authed)/sessions/[id]/game-client.tsx — lines ~238 and ~297 (the already-present `void refetch()`), to confirm the durable client recovery exists for the verification note in this task's done criteria.
  </read_first>
  <behavior>
    Assert the branch decision the empty-narration else makes, expressed as the smallest correct unit (option b — do NOT claim a full route drive, since the route has no unit harness):
    - Guard expression: `combatStateChanged = _resolver !== null || _monsterLoopRan || openerRan` is true when ANY of (_resolver non-null, _monsterLoopRan true, openerRan true), and false only when all three are falsy. Drive a small truth-table over these inputs and assert the resulting boolean.
    - Branch outcome — COMBAT empty turn (combatStateChanged true): the chosen notify is `{type:'state'}` AND the `{type:'turn-error'}` notify is NOT chosen (state XOR turn-error; state fires, turn-error does not).
    - Branch outcome — NON-COMBAT empty turn (combatStateChanged false, i.e. _resolver null AND _monsterLoopRan false AND openerRan false): the chosen notify is `{type:'turn-error', reason:'empty_response'}` AND the `{type:'state'}` notify is NOT chosen (turn-error fires, state does not). This preserves the existing retry UX for genuine model failures.
    - Mechanism note (no assertion needed, stated in done): {type:'state'} is what use-session-stream.ts:113-116 maps to a silent refetch(); {type:'turn-error'} is what 120-134 maps to refetch()+error toast; game-client.tsx:238/297 already fire void refetch() for the durable/dropped-SSE path.
  </behavior>
  <action>
    Write the test mirroring the turn/ pure-function conventions. Because route.ts has no unit-test harness and a full route drive is not cheap, model the empty-narration branch as a tiny local decision helper in the TEST (or, if Task 1 happens to factor the decision into a small pure exported helper, import that) — the assertion is on the branch logic, NOT on a live route call:
    1. Encode the guard: `combatStateChanged = _resolver !== null || _monsterLoopRan || openerRan`. Drive the truth table: (_resolver set / _monsterLoopRan true / openerRan true) each independently → true; all falsy → false.
    2. Encode the branch outcome: when combatStateChanged is true the emitted event is {type:'state'} and turn-error is suppressed; when false the emitted event is {type:'turn-error', reason:'empty_response'} and state is suppressed. Assert EXACTLY ONE event per case (state XOR turn-error).
    Do NOT spin up a live Postgres, an HTTP server, or claim a full headless route handler invocation — the unit under test is the branch decision. Run GREEN. Commit: `test(10): empty-narration branch — combat→state (no turn-error), non-combat→turn-error (no state)`.
    Separately, as a verification step for the downgraded Option C: open game-client.tsx and confirm `void refetch()` is present at ~238 and ~297; if and only if a concrete gap in the durable/dropped-SSE recovery is found there, note it in the SUMMARY and propose a minimal extension — otherwise record "client durable recovery already shipped, no change needed."
  </action>
  <verify>
    <automated>npx vitest run tests/app/api/sessions/[id]/turn/empty-narration-notify.test.ts 2>&1 | grep -qE "passed|PASS" && ! npx vitest run tests/app/api/sessions/[id]/turn/empty-narration-notify.test.ts 2>&1 | grep -qE "[1-9][0-9]* failed" && echo NOTIFY_TEST_OK</automated>
  </verify>
  <verify>
    <automated>grep -cE "void refetch\(\)" "src/app/(authed)/sessions/[id]/game-client.tsx" | grep -qE "[2-9]|[1-9][0-9]" && echo CLIENT_RECOVERY_PRESENT</automated>
  </verify>
  <done>The test asserts the branch decision: combatStateChanged is true iff any of _resolver/_monsterLoopRan/openerRan is truthy; a combat empty turn (true) yields {type:'state'} and NOT turn-error; a non-combat empty turn (false) yields {type:'turn-error', reason:'empty_response'} and NOT state (exactly one event per case). It does NOT claim a full route drive (route has no unit harness). The grep confirms game-client.tsx still has the already-shipped `void refetch()` recovery (≥2 occurrences), and the SUMMARY records whether any client extension was needed (default: none).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| route → notifySession (Postgres NOTIFY → SSE → client) | The empty-narration branch crosses into the realtime notify channel; only server-confirmed combat-state changes should emit the silent {type:'state'} refresh, and only genuine non-combat empties should emit the {type:'turn-error'} retry, to prevent both a refresh-storm DoS and spurious error toasts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-10 | Denial of Service | unconditional state notify on every empty turn | mitigate | The state notify fires ONLY when combatStateChanged (_resolver !== null || _monsterLoopRan || openerRan); non-combat empty turns emit the original turn-error path, never an extra state refresh. Exactly one notify per empty turn (state XOR turn-error). Negative test enforces this. |
| T-10-11 | Spoofing | notify targeting wrong session | mitigate | notifySession is called with the route's own sessionId (the authenticated turn's session); no client-supplied target. |
| T-10-12 | Information disclosure | {type:'state'} payload | accept | The state event carries no data — it only signals the client to refetch its own authorized snapshot; no secret crosses the channel. |
| T-10-14 | Tampering (UX integrity) | bogus error toast on a successful silent combat turn | mitigate | A combat turn that advanced state emits {type:'state'} INSTEAD OF {type:'turn-error'}, so the client does not show the "no response / retry" toast for a turn that actually succeeded; the turn-error path is reserved for genuine non-combat model failures. |
</threat_model>

<verification>
- Both task verify commands pass.
- `grep -nE "notifySession\(.*type:.*'state'" src/app/api/sessions/[id]/turn/route.ts` (comment-filtered) matches inside the empty-narration branch, AND the original {type:'turn-error', reason:'empty_response'} emit is still present (on the combatStateChanged-false branch).
- `grep -c "void refetch()" "src/app/(authed)/sessions/[id]/game-client.tsx"` ≥ 2 (client recovery still shipped, untouched).
- use-session-stream.ts is NOT in this plan's files_modified.
</verification>

<success_criteria>
- REQ-046 empty-narration gap closed without UX regression: a server-resolved combat turn with empty narration emits {type:'state'} (silent refetch → tracker updates) INSTEAD OF the turn-error notify, so no bogus "no response / retry" toast appears on a turn that succeeded.
- A genuine non-combat empty turn still emits the existing {type:'turn-error', reason:'empty_response'} (retry UX preserved).
- Exactly one notify per empty turn (state XOR turn-error); no refresh storm. No client file edited; the already-shipped game-client.tsx recovery is confirmed, not re-implemented.
</success_criteria>

<output>
Create `.planning/phases/10-server-authoritative-combat-and-tracker/10-04-SUMMARY.md` when done.
</output>
