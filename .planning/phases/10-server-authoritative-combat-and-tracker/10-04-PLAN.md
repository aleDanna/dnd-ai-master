---
phase: 10-server-authoritative-combat-and-tracker
plan: 04
type: execute
wave: 3
depends_on: [10-03]
files_modified:
  - src/app/api/sessions/[id]/turn/route.ts
  - tests/app/api/sessions/[id]/turn/empty-narration-notify.test.ts
autonomous: true
requirements: [REQ-046]
must_haves:
  truths:
    - "When a server-resolved combat turn produces empty narration, the route emits notifySession(sessionId, {type:'state'}) so the client refetches and the tracker updates"
    - "The state-notify in the empty-narration else branch is guarded by combatStateChanged = _resolver !== null || _monsterLoopRan || openerRan, so non-combat empty turns do not spam refreshes"
    - "The {type:'state'} event the client already maps to refetch() (use-session-stream.ts:113-116) is the mechanism — no new client code is added to a file that already recovers"
  artifacts:
    - path: "src/app/api/sessions/[id]/turn/route.ts"
      provides: "notifySession({type:'state'}) in the empty-narration else branch, guarded by combatStateChanged"
      contains: "notifySession"
    - path: "tests/app/api/sessions/[id]/turn/empty-narration-notify.test.ts"
      provides: "Test: monster-loop turn with empty LLM narration fires the {type:'state'} notify; a non-combat empty turn does not"
      contains: "type: 'state'"
  key_links:
    - from: "src/app/api/sessions/[id]/turn/route.ts (empty-narration else branch ~748-755)"
      to: "src/sessions/notify.ts notifySession(sessionId, {type:'state'})"
      via: "guarded emit when combatStateChanged"
      pattern: "notifySession.*state"
    - from: "src/sessions/notify.ts {type:'state'} event"
      to: "src/sessions/use-session-stream.ts:113-116 → refetch()"
      via: "EventSource state handler the client already maps to refetch"
      pattern: "refetch"
---

<objective>
Close the residual REQ-046 tracker-refresh gap for the EMPTY-NARRATION case. The client-side snapshot recovery is ALREADY SHIPPED: game-client.tsx fires `void refetch()` at ~238 (finalizedSeq effect) and ~297 (safety poll), and use-session-stream.ts:113-116 already maps a `{type:'state'}` SSE event to `refetch()`. The remaining hole is server-side: when a server-resolved combat turn produces EMPTY narration, no master message is persisted, so the safety poll's `masterCount > baseline` condition never trips and its refetch never fires — the tracker silently stays stale after a server-authoritative HP/initiative change.

The load-bearing REQUIRED fix is Option S: in route.ts's empty-narration `else` branch (~748-755), emit `notifySession(sessionId, {type:'state'})`, guarded by `combatStateChanged = _resolver !== null || _monsterLoopRan || openerRan`. The client already converts `{type:'state'}` to a refetch, so this single server emit makes the tracker update even when the turn says nothing.

Option C is explicitly DOWNGRADED to a verification step: confirm the already-present `void refetch()` at game-client.tsx:238/297 covers the dropped-SSE / durable path; extend ONLY if a concrete gap is found — do NOT plan a from-scratch client change to a file that already recovers.

Purpose: A server-authoritative combat change with no narration still refreshes the tracker.
Output: one guarded server-side notify + a focused test; a verification note on the already-shipped client recovery.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-server-authoritative-combat-and-tracker/10-CONTEXT.md
@.planning/phases/10-server-authoritative-combat-and-tracker/10-RESEARCH.md
@src/app/api/sessions/[id]/turn/route.ts
@src/sessions/notify.ts
@src/sessions/use-session-stream.ts
@src/app/(authed)/sessions/[id]/game-client.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Emit guarded {type:'state'} notify in the empty-narration else branch</name>
  <files>src/app/api/sessions/[id]/turn/route.ts</files>
  <read_first>
    - src/app/api/sessions/[id]/turn/route.ts — CONFIRMED anchors: the empty-narration `else` branch ~748-755 (the path taken when the LLM returns no narration and no master message is persisted); `_resolver` declared ~337, `_monsterLoopRan` declared ~399, and `openerRan` declared at the vault-branch scope by 10-03 (a boolean). All three are in scope at ~748.
    - src/sessions/notify.ts — CONFIRMED notifySession(sessionId, payload) where payload.type may be 'state' (~line 8). This is the exact call to emit.
    - src/sessions/use-session-stream.ts — CONFIRMED 113-116: the EventSource handler maps a {type:'state'} event to refetch(). This is the client mechanism the server emit relies on; READ ONLY — do not edit this file (it has no startSafetyPoll/finalizedSeq; those live in game-client.tsx).
    - .planning/phases/10-server-authoritative-combat-and-tracker/10-CONTEXT.md — REQ-046 tracker-refresh decision; v1/v2 stack untouched (this plan only ADDS a notify in the else branch; it does not edit combat-resolver/monster-turns/projector math).
  </read_first>
  <action>
    In route.ts's empty-narration `else` branch (~748-755), add: compute `const combatStateChanged = _resolver !== null || _monsterLoopRan || openerRan;` (read the three existing in-scope signals — _resolver from ~337, _monsterLoopRan from ~399, openerRan the boolean 10-03 declared at the vault-branch scope). When `combatStateChanged` is true, call `notifySession(sessionId, { type: 'state' })` (import notifySession if not already imported in this file). This guard ensures only server-resolved COMBAT turns with empty narration trigger a refresh — a plain non-combat empty turn (all three signals false) emits nothing, avoiding refresh spam.
    Do NOT touch use-session-stream.ts or game-client.tsx (client recovery is already shipped). Do NOT edit any v1/v2 file. The only change is the additive guarded notify in the else branch.
  </action>
  <verify>
    <automated>grep -nE "notifySession\([^)]*type:[[:space:]]*'state'" "src/app/api/sessions/[id]/turn/route.ts" | grep -v '^[0-9]*:[[:space:]]*//' | grep -qE "." &amp;&amp; grep -qE "combatStateChanged[[:space:]]*=[^;]*_resolver[^;]*_monsterLoopRan[^;]*openerRan" "src/app/api/sessions/[id]/turn/route.ts" &amp;&amp; echo NOTIFY_OK</automated>
  </verify>
  <done>The empty-narration else branch computes `combatStateChanged = _resolver !== null || _monsterLoopRan || openerRan` and calls `notifySession(sessionId, {type:'state'})` when true. use-session-stream.ts and game-client.tsx are unmodified. No v1/v2 file edited.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test the empty-narration notify (positive: monster-loop + empty LLM → state notify; negative: non-combat empty → no notify) + verify client recovery already present</name>
  <files>tests/app/api/sessions/[id]/turn/empty-narration-notify.test.ts</files>
  <read_first>
    - src/sessions/notify.ts — notifySession signature, to mock/spy it and assert the {type:'state'} payload.
    - src/app/api/sessions/[id]/turn/route.ts — the empty-narration branch and the _resolver/_monsterLoopRan/openerRan guard, to know which conditions to drive.
    - tests/app/api/sessions/[id]/turn/combat-resolver.test.ts — the turn/ test conventions (mock the LLM, drive the branch).
    - src/app/(authed)/sessions/[id]/game-client.tsx — lines ~238 and ~297 (the already-present `void refetch()`), to confirm the durable client recovery exists for the verification note in this task's done criteria.
  </read_first>
  <behavior>
    - Positive (the gap this plan fixes — WARNING 5): drive a server-resolved turn where `_monsterLoopRan === true` (or the v1 `_resolver` damage branch) AND the mocked LLM returns '' (empty narration) → assert notifySession was called once with (sessionId, {type:'state'}). This is the realistic empty-narration combat trigger; do NOT use an opener-only turn as the canonical empty case because openers normally narrate.
    - Negative: drive an empty-narration turn where _resolver is null AND _monsterLoopRan is false AND openerRan is false (a plain non-combat empty turn) → assert notifySession was NOT called with {type:'state'} (no refresh spam).
    - Mechanism note (no assertion needed, but stated in done): {type:'state'} is the event use-session-stream.ts:113-116 already maps to refetch(); and game-client.tsx:238/297 already fire void refetch() for the durable/dropped-SSE path.
  </behavior>
  <action>
    Write the test mirroring the turn/ conventions. Spy/mock notifySession (vi.mock the notify module, capture calls). Mock the LLM to return '' so the route enters the empty-narration else branch. Construct the route invocation for the positive case so _monsterLoopRan is true (a server-resolved monster turn) and assert notifySession was called with (sessionId, {type:'state'}). Construct the negative case as a non-combat empty turn (all three guard signals false) and assert no {type:'state'} notify. Keep the test headless — do not require a live Postgres; the assertion is on the mocked notifySession call, not on a real NOTIFY. Run GREEN. Commit: `test(10): empty-narration combat turn fires state notify; non-combat does not`.
    Separately, as a verification step for the downgraded Option C: open game-client.tsx and confirm `void refetch()` is present at ~238 and ~297; if and only if a concrete gap in the durable/dropped-SSE recovery is found there, note it in the SUMMARY and propose a minimal extension — otherwise record "client durable recovery already shipped, no change needed."
  </action>
  <verify>
    <automated>npx vitest run tests/app/api/sessions/[id]/turn/empty-narration-notify.test.ts 2>&1 | grep -qE "passed|PASS" &amp;&amp; ! npx vitest run tests/app/api/sessions/[id]/turn/empty-narration-notify.test.ts 2>&1 | grep -qE "[1-9][0-9]* failed" &amp;&amp; echo NOTIFY_TEST_OK</automated>
  </verify>
  <verify>
    <automated>grep -cE "void refetch\(\)" "src/app/(authed)/sessions/[id]/game-client.tsx" | grep -qE "[2-9]|[1-9][0-9]" &amp;&amp; echo CLIENT_RECOVERY_PRESENT</automated>
  </verify>
  <done>Positive test asserts a monster-loop (_monsterLoopRan) empty-narration turn calls notifySession(sessionId,{type:'state'}); negative test asserts a non-combat empty turn does not. The grep confirms game-client.tsx still has the already-shipped `void refetch()` recovery (≥2 occurrences), and the SUMMARY records whether any client extension was needed (default: none).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| route → notifySession (Postgres NOTIFY → SSE → client) | The empty-narration branch crosses into the realtime notify channel; only server-confirmed combat-state changes should emit, to prevent a refresh-storm DoS on connected clients. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-10 | Denial of Service | unconditional state notify on every empty turn | mitigate | The notify is guarded by combatStateChanged (_resolver !== null || _monsterLoopRan || openerRan); non-combat empty turns emit nothing. Negative test enforces this. |
| T-10-11 | Spoofing | notify targeting wrong session | mitigate | notifySession is called with the route's own sessionId (the authenticated turn's session); no client-supplied target. |
| T-10-12 | Information disclosure | {type:'state'} payload | accept | The state event carries no data — it only signals the client to refetch its own authorized snapshot; no secret crosses the channel. |
</threat_model>

<verification>
- Both task verify commands pass.
- `grep -nE "notifySession\(.*type:.*'state'" src/app/api/sessions/[id]/turn/route.ts` (comment-filtered) matches inside the empty-narration branch.
- `grep -c "void refetch()" "src/app/(authed)/sessions/[id]/game-client.tsx"` ≥ 2 (client recovery still shipped, untouched).
- use-session-stream.ts is NOT in this plan's files_modified.
</verification>

<success_criteria>
- REQ-046 empty-narration gap closed: a server-resolved combat turn with empty narration emits {type:'state'}, which the client already maps to refetch(), so the tracker updates.
- The notify is guarded so non-combat empty turns do not spam refreshes.
- No client file edited; the already-shipped game-client.tsx recovery is confirmed, not re-implemented.
</success_criteria>

<output>
Create `.planning/phases/10-server-authoritative-combat-and-tracker/10-04-SUMMARY.md` when done.
</output>
