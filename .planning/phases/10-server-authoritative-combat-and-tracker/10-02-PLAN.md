---
phase: 10-server-authoritative-combat-and-tracker
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/app/api/sessions/[id]/turn/monster-bestiary.ts
  - tests/app/api/sessions/[id]/turn/monster-bestiary-statblock.test.ts
autonomous: true
requirements: [REQ-045]
must_haves:
  truths:
    - "getBestiaryStatblock('goblin') returns the goblin's real seeded stats {hpMax:7, ac:15, cr:'1/4'} read from data/vault/handbook/monsters/goblin.md"
    - "getBestiaryStatblock(name) returns null when no statblock file exists for that monster — it never throws"
    - "The reader resolves the monster file through safeVaultPath/readVaultFile and never string-concatenates a filesystem path (traversal/ReDoS-safe)"
  artifacts:
    - path: "src/app/api/sessions/[id]/turn/monster-bestiary.ts"
      provides: "NEW exported getBestiaryStatblock(name) → {hpMax?, ac?, cr?} | null colocated with getBestiaryAttackStats; existing functions unchanged"
      exports: ["getBestiaryStatblock", "getBestiaryAttackStats"]
      contains: "getBestiaryStatblock"
    - path: "tests/app/api/sessions/[id]/turn/monster-bestiary-statblock.test.ts"
      provides: "RED→GREEN tests asserting goblin real stats from a seeded file + null for unknown monster"
      contains: "getBestiaryStatblock"
  key_links:
    - from: "src/app/api/sessions/[id]/turn/monster-bestiary.ts"
      to: "data/vault/handbook/monsters/<slug>.md frontmatter"
      via: "safeVaultPath + readVaultFile (no string concat)"
      pattern: "safeVaultPath|readVaultFile"
---

<objective>
Add the missing SRD statblock reader that REQ-045 needs to spawn monsters with their real seeded HP/AC/CR. Today `getBestiaryAttackStats(name)` returns only attack prose ({attackBonus, damageDice}); there is NO reader that returns {hpMax, ac, cr}. Without it, the opener's bestiaryLookup always returns null and EVERY monster — goblin and dragon alike — gets a generic CR-default HP, silently degrading REQ-045 ("select monster stats from the seeded SRD bestiary") to "CR-guess for everything."

This plan adds a NEW exported function `getBestiaryStatblock(name) → { hpMax?: number; ac?: number; cr?: string } | null`, colocated in monster-bestiary.ts, that reads `handbook/monsters/<slug>.md` through the same `safeVaultPath`/`readVaultFile` helpers `getBestiaryAttackStats` already uses (path-traversal/ReDoS-safe — NEVER string-concat a path), parses the frontmatter `hp`/`ac`/`cr` fields, and maps `hp`→`hpMax`. Adding a new exported reader is explicitly allowed by the v1/v2-untouched constraint; the existing `getBestiaryAttackStats` math/parse is NOT edited.

This is the second Wave-1 plan (parallel with 10-01). It plugs into the opener as the injected `bestiaryLookup` (10-01) and is wired into production by the route (10-03). Splitting it out also resolves the "10-02 was too heavy" warning by giving the bestiary-stats path its own focused TDD plan.

Purpose: Make REQ-045 real — the goblin spawns with hpMax 7 from the SRD, not a CR guess.
Output: NEW `getBestiaryStatblock` export + its test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-server-authoritative-combat-and-tracker/10-CONTEXT.md
@.planning/phases/10-server-authoritative-combat-and-tracker/10-RESEARCH.md
@src/app/api/sessions/[id]/turn/monster-bestiary.ts
@data/vault/handbook/monsters/goblin.md
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — pin getBestiaryStatblock to the goblin's REAL seeded frontmatter</name>
  <files>tests/app/api/sessions/[id]/turn/monster-bestiary-statblock.test.ts</files>
  <read_first>
    - data/vault/handbook/monsters/goblin.md — CONFIRMED frontmatter: `hp: 7`, `ac: 15`, `cr: "1/4"`. The assertion is goblin → {hpMax:7, ac:15, cr:"1/4"}. The frontmatter keys are `hp`/`ac`/`cr` (NOT hpMax); the reader maps hp→hpMax. Do NOT guess — these are the verified values.
    - tests/ai/master/vault/projector.test.ts — the fs-seed pattern: mkdtempSync(tmpdir prefix) + vi.stubEnv('VAULT_CAMPAIGNS_ROOT', dir) + writeFileSync. (Use this ONLY if getBestiaryStatblock reads through VAULT_CAMPAIGNS_ROOT. See note below — the bestiary lives under data/vault/handbook, which is a repo-relative read, so first confirm whether monster-bestiary.ts reads the committed handbook path directly or via an env root.)
    - src/app/api/sessions/[id]/turn/monster-bestiary.ts — how getBestiaryAttackStats resolves the file (it imports safeVaultPath + readVaultFile and reads `handbook/monsters/${slug}.md`). The new test must exercise the SAME resolution so the assertion proves the real committed goblin.md is read, not a stub.
  </read_first>
  <behavior>
    - getBestiaryStatblock('goblin') → an object deep-equal to { hpMax: 7, ac: 15, cr: '1/4' } (values read from the committed data/vault/handbook/monsters/goblin.md). This is the canonical REAL-stats assertion.
    - getBestiaryStatblock('Goblin') and getBestiaryStatblock(' goblin ') (case/whitespace) → still resolve goblin (slugify/normalize), returning the same stats — proving the name-normalization matches getBestiaryAttackStats's slug logic.
    - getBestiaryStatblock('no-such-monster-xyz') → null (no file → null, never a throw).
    - Path safety: getBestiaryStatblock('../../etc/passwd') → null (safeVaultPath rejects traversal; assert no throw and null/empty result).
  </behavior>
  <action>
    Write the failing test mirroring the turn/ test conventions. Import getBestiaryStatblock from "../../../../../../src/app/api/sessions/[id]/turn/monster-bestiary" (match the relative depth of sibling turn tests). Because the bestiary file is COMMITTED at data/vault/handbook/monsters/goblin.md, the goblin assertions read the real repo file directly — no fs seeding needed for the goblin case. Add the four behaviors as it() blocks. If, after reading monster-bestiary.ts, you find the reader resolves via VAULT_CAMPAIGNS_ROOT rather than the repo handbook path, THEN seed a goblin.md into a mkdtempSync dir with vi.stubEnv('VAULT_CAMPAIGNS_ROOT', dir) per projector.test.ts and write the same hp/ac/cr frontmatter — but default to the direct committed-file read since getBestiaryAttackStats reads handbook/monsters directly. Run the suite; confirm it FAILS because getBestiaryStatblock is not exported yet. Commit: `test(10): add failing getBestiaryStatblock tests against real goblin SRD`.
  </action>
  <verify>
    <automated>npx vitest run tests/app/api/sessions/[id]/turn/monster-bestiary-statblock.test.ts 2>&1 | grep -qE "fail|FAIL|Cannot find module|is not a function|No test files" && echo RED_OK</automated>
  </verify>
  <done>Test file exists with the four it() blocks; the goblin case asserts `toEqual({ hpMax: 7, ac: 15, cr: '1/4' })`; the suite fails because getBestiaryStatblock is unimplemented.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN — implement getBestiaryStatblock (path-safe, frontmatter hp/ac/cr → {hpMax,ac,cr})</name>
  <files>src/app/api/sessions/[id]/turn/monster-bestiary.ts</files>
  <read_first>
    - src/app/api/sessions/[id]/turn/monster-bestiary.ts — reuse the EXACT file-resolution + frontmatter-parse approach getBestiaryAttackStats already uses: the slug normalization, safeVaultPath(`handbook/monsters/${slug}.md`), readVaultFile, and the existing frontmatter parser. Add the new function ALONGSIDE getBestiaryAttackStats; do NOT modify getBestiaryAttackStats or its parser.
    - data/vault/handbook/monsters/goblin.md — confirms the frontmatter field names hp/ac/cr to read.
  </read_first>
  <action>
    Add and export `getBestiaryStatblock(name: string): { hpMax?: number; ac?: number; cr?: string } | null`. Implementation:
    1. Normalize/slugify the name with the SAME logic getBestiaryAttackStats uses (lowercase, trim, spaces→hyphens).
    2. Resolve the file via safeVaultPath(`handbook/monsters/${slug}.md`) — reuse the helper; NEVER concatenate a raw path. If safeVaultPath rejects the input (traversal) return null.
    3. readVaultFile the resolved path; if it does not exist / read fails, return null (no throw).
    4. Parse the frontmatter using the module's existing parse path; read `hp`, `ac`, `cr`. Map hp→hpMax (Number), ac→ac (Number), cr→cr (string, preserve "1/4"). Omit any field that is absent rather than emitting NaN.
    5. Return the assembled object, or null if no recognizable stats were found.
    Do NOT edit any v1/v2 file (combat-resolver, monster-turns, combat-handoff, projector, events-schema) and do NOT change getBestiaryAttackStats. Run the test suite to GREEN. Then run the existing bestiary/turn suite to prove getBestiaryAttackStats still passes. Commit: `feat(10): add getBestiaryStatblock SRD stats reader`.
  </action>
  <verify>
    <automated>npx vitest run tests/app/api/sessions/[id]/turn/monster-bestiary-statblock.test.ts 2>&1 | grep -qE "passed|PASS" && ! npx vitest run tests/app/api/sessions/[id]/turn/monster-bestiary-statblock.test.ts 2>&1 | grep -qE "[1-9][0-9]* failed" && echo GREEN_OK</automated>
  </verify>
  <done>getBestiaryStatblock is exported and all four it() blocks pass: goblin → {hpMax:7, ac:15, cr:'1/4'}, case/whitespace normalize to goblin, unknown monster → null, traversal input → null (no throw). getBestiaryAttackStats and all v1/v2 files are unmodified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| monster name (LLM/route-supplied) → filesystem read | The name is an untrusted string used to locate a file under handbook/monsters; a naive concat would allow `../` traversal or a ReDoS-prone path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | Tampering | path built from monster name | mitigate | Resolve exclusively through safeVaultPath(`handbook/monsters/${slug}.md`) + readVaultFile (the existing traversal-safe helpers); slugify first; reject/return null on traversal input. No string concatenation of raw input into a path. |
| T-10-05 | Denial of Service | malformed/huge frontmatter | mitigate | Parse with the existing frontmatter parser and coerce only hp/ac/cr; return null on read/parse failure rather than throwing, so a bad file cannot crash the turn route. |
| T-10-06 | Information disclosure | reading arbitrary files | mitigate | safeVaultPath confines reads to the vault handbook tree; traversal inputs return null. |
</threat_model>

<verification>
- Both vitest commands pass (RED then GREEN).
- `grep -n "getBestiaryStatblock" src/app/api/sessions/[id]/turn/monster-bestiary.ts` shows the new export; `grep -n "safeVaultPath" src/app/api/sessions/[id]/turn/monster-bestiary.ts` shows the new function uses it (no raw concat).
- The existing getBestiaryAttackStats tests still pass (no regression to v1/v2 reader).
</verification>

<success_criteria>
- REQ-045 stats path exists: getBestiaryStatblock('goblin') returns the real seeded {hpMax:7, ac:15, cr:'1/4'}.
- Reader is path-safe (safeVaultPath), returns null on missing/traversal, never throws.
- getBestiaryAttackStats and all v1/v2 combat files unchanged.
</success_criteria>

<output>
Create `.planning/phases/10-server-authoritative-combat-and-tracker/10-02-SUMMARY.md` when done.
</output>
