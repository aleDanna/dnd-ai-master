---
phase: 10-server-authoritative-combat-and-tracker
plan: 03
type: execute
wave: 2
depends_on: [10-01, 10-02]
files_modified:
  - src/app/api/sessions/[id]/turn/route.ts
  - tests/app/api/sessions/[id]/turn/encounter-opener-wiring.test.ts
autonomous: true
requirements: [REQ-045, REQ-047]
must_haves:
  truths:
    - "On a vault turn where combat intent is detected and no encounter is active and the result is not a roll, the route calls runEncounterOpener and dispatches its monster_spawn + initiative_set via dispatchVaultTool with BOTH campaignId and sessionId"
    - "The opener trigger is gated by vaultMutations && !encounter.active && detectCombatIntent && !isRollResult and is ordered AFTER the isRollResult check"
    - "A 'goblin' combat-intent turn spawns the goblin with its REAL bestiary hpMax (7), not a CR-default — proving getBestiaryStatblock is wired as the opener's bestiaryLookup"
    - "The route never emits a damage event on the opening turn (REQ-047)"
    - "A boolean openerRan is declared at the vault-branch scope alongside _resolver/_monsterLoopRan and set true when the opener runs, so the empty-narration branch (10-04) can read it"
  artifacts:
    - path: "src/app/api/sessions/[id]/turn/route.ts"
      provides: "Opener trigger + dispatch wiring; getBestiaryStatblock injected as bestiaryLookup; openerRan cross-plan signal"
      contains: "runEncounterOpener"
    - path: "tests/app/api/sessions/[id]/turn/encounter-opener-wiring.test.ts"
      provides: "Headless wiring test: goblin intent → real goblin hpMax in monster_spawn + initiative order includes PC UUID(s)+monster id; no real Postgres NOTIFY"
      contains: "getBestiaryStatblock"
  key_links:
    - from: "src/app/api/sessions/[id]/turn/route.ts"
      to: "src/app/api/sessions/[id]/turn/encounter-opener.ts (runEncounterOpener)"
      via: "call at the gated combat-intent branch, passing getBestiaryStatblock as bestiaryLookup"
      pattern: "runEncounterOpener"
    - from: "src/app/api/sessions/[id]/turn/route.ts"
      to: "src/app/api/sessions/[id]/turn/monster-bestiary.ts (getBestiaryStatblock)"
      via: "injected as the opener's bestiaryLookup"
      pattern: "getBestiaryStatblock"
    - from: "src/app/api/sessions/[id]/turn/route.ts"
      to: "src/ai/master/vault/tools.ts (dispatchVaultTool)"
      via: "dispatch monster_spawn + initiative_set with campaignId AND sessionId"
      pattern: "dispatchVaultTool"
---

<objective>
Wire the server-authoritative opener into the live turn route. When a vault turn detects combat intent, no encounter is active, and the turn is not a roll result, the route calls runEncounterOpener (10-01) with getBestiaryStatblock (10-02) injected as the bestiaryLookup, then dispatches the returned monster_spawn + initiative_set through dispatchVaultTool with BOTH campaignId and sessionId. This completes REQ-045 end to end (server selects the monster from the SRD bestiary and rolls initiative) and holds the REQ-047 invariant (no damage on the opener turn).

It also plants the cross-plan signal openerRan (a boolean at the vault-branch scope, alongside _resolver/_monsterLoopRan) that 10-04's empty-narration notifySession guard reads.

Purpose: Turn the tested pure pieces into a working in-game encounter open, proving the REAL bestiary path is wired (goblin spawns at hpMax 7, not a CR guess).
Output: route wiring + a headless integration test that fires no real Postgres NOTIFY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-server-authoritative-combat-and-tracker/10-CONTEXT.md
@.planning/phases/10-server-authoritative-combat-and-tracker/10-RESEARCH.md
@src/app/api/sessions/[id]/turn/route.ts
@src/app/api/sessions/[id]/turn/encounter-opener.ts
@src/app/api/sessions/[id]/turn/monster-bestiary.ts
@src/ai/master/vault/tools.ts
@src/ai/master/vault/turn-directive.ts
@src/ai/master/vault/projector.ts

# Next.js is non-standard here — read before touching route-handler code
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire the gated opener trigger + dispatch + openerRan signal into route.ts</name>
  <files>src/app/api/sessions/[id]/turn/route.ts</files>
  <read_first>
    - src/app/api/sessions/[id]/turn/route.ts — CONFIRMED anchors: the vault gate (masterBackend === 'vault' ~235; vaultMutationsEnabled ~239); const isBegin = body?.begin === true ~55; the clean _playerMessage capture ~316-318; _resolver declared ~337; the _resolver EMIT loop ~375-385; _monsterLoopRan declared ~399; the empty-narration else branch ~748-755 (10-04 territory). The opener hook goes BETWEEN the _playerMessage capture (~318) and the v1 _resolver gate (~337) so the v1/v2 gates' replayEvents reads (340 / 407) see the opened encounter. The opener must gate on the REAL player turn (!isBegin) — the begin branch builds a synthetic user message (~278-285) and must not trip the opener.
    - src/app/api/sessions/[id]/turn/encounter-opener.ts — runEncounterOpener(snapshot, monsterName, bestiaryLookup) signature + return shape from 10-01 (it returns [] for empty party, [monster_spawn, initiative_set] otherwise; never throws).
    - src/app/api/sessions/[id]/turn/monster-bestiary.ts — getBestiaryStatblock(name) from 10-02 returns {hpMax?,ac?,cr?}|null. NOTE it is async (it awaits readVaultFile, mirroring getBestiaryAttackStats). Resolve its result BEFORE the pure runEncounterOpener call and pass a synchronous lookup closure (see action). getBestiaryAttackStats returns ONLY {attackBonus, damageDice} — it is NOT the stats reader; use getBestiaryStatblock.
    - src/ai/master/vault/turn-directive.ts — detectCombatIntent(playerMessage) (~81-84) and isRollResult(playerMessage) (~91-94); both already imported in route.ts (isRollResult at line 42). detectCombatIntent currently only steers the directive — reuse it as the opener gate; it surfaces attack intent, not a fully-parsed monster name, so the route derives the monster name from the player message (see action).
    - src/ai/master/vault/tools.ts — dispatchVaultTool('apply_event', ev, {campaignId, sessionId}) (~212); it must receive campaignId AND sessionId in production; UUID guard at ~288 (campaignId must be a real UUID); emitStateRefresh is a no-op when sessionId is absent (~200-205). Mirror the EXACT v1 EMIT loop pattern at route.ts ~375-385 (await dispatch; log on r.isError).
    - src/ai/master/vault/projector.ts — replayEvents(parseEventsFile(eventsPath(campaign.id))) (~849) returns {encounter, chars}; EncounterState ~661. The opener reads the FRESH encounter to gate on !encounter.active.
    - .planning/phases/10-server-authoritative-combat-and-tracker/10-CONTEXT.md — LOCKED: Option B only (no constrained-JSON/Ollama format opener — deferred; keep monster-selection swappable for future option A); monster_spawn {id,name,hpMax} + initiative_set order (PC UUID(s)+monster id); dispatch with campaignId AND sessionId; REQ-047 no damage on opener; v1/v2 stack untouched.
  </read_first>
  <action>
    Declare let openerRan = false; at the SAME vault-branch scope where _resolver (~337) and _monsterLoopRan (~399) are declared — it MUST be a boolean (to match 10-04's combatStateChanged = _resolver !== null || _monsterLoopRan || openerRan guard) and visible to the empty-narration else branch ~390 lines below (~748). This identifier name and scope are a cross-plan contract with 10-04 — name it exactly openerRan.

    Add the opener trigger BETWEEN the _playerMessage capture (~318) and the v1 _resolver gate (~337), AFTER the isRollResult check ordering (the gate itself includes !isRollResult). Gate the whole block on !isBegin AND exactly: vaultMutationsEnabled && !encounter.active && detectCombatIntent(_playerMessage) && !isRollResult(_playerMessage). Read the fresh encounter via replayEvents(await parseEventsFile(eventsPath(campaign.id))) (the same read pattern the v1 gate uses) to evaluate !encounter.active. Document inline that isRollResult is the REQ-047 / loop-avoidance ordering requirement (a roll-result echoes the attack verb and would otherwise re-trip detectCombatIntent).

    Inside the gate:
    1. Derive the monster name from _playerMessage using the existing combat-intent signal (Option B — do NOT add a constrained-JSON/Ollama format detector; that is the deferred option A). Keep the name-derivation isolated so option A can later replace ONLY this step.
    2. Resolve stats first (getBestiaryStatblock is async): const stats = await getBestiaryStatblock(monsterName); then call runEncounterOpener(snap, monsterName, () => stats) — pass a synchronous closure returning the pre-fetched {hpMax?,ac?,cr?}|null so the pure opener stays synchronous. This wires the REAL bestiary path: a "goblin" turn resolves to hpMax 7 from the SRD; the opener's own null-fallback covers unknown monsters.
    3. For each event runEncounterOpener returns, dispatch via dispatchVaultTool('apply_event', ev, { campaignId: campaign.id, sessionId }) — pass BOTH the real campaign UUID AND sessionId so emitStateRefresh fires in production and the tracker updates. Log on r.isError exactly like the v1 EMIT loop (~381-383). monster_spawn carries {id,name,hpMax}; initiative_set.order includes the PC UUID(s) + the monster id.
    4. Set openerRan = true; after the opener events are dispatched.
    Wrap the whole hook in try/catch mirroring the v1/v2 defensive wrapping (D-10): on error console.warn('[turn]', sessionId, 'encounter opener failed, falling through:', ...) and leave openerRan false — never hard-fail the turn.

    Add a one-line code comment noting per INFO-9 that snap.party rows carry no initiativeBonus/initiative field (the characters schema has ac/hpMax only), so PC initiative is 1d20+0 by design inside the opener — intentional, not dropped wiring.

    Do NOT emit any damage on this turn (REQ-047). Do NOT edit v1/v2 files (combat-resolver / monster-turns / monster-bestiary existing functions / combat-handoff / projector / events-schema). Do NOT add a constrained-JSON/Ollama format path. Do NOT collapse the existing duplicate events.md reads (the v1 gate at ~340 and v2 gate at ~407 must still run AFTER the opener persists, so they see the opened encounter).
  </action>
  <verify>
    <automated>grep -nE "openerRan|runEncounterOpener|getBestiaryStatblock" "src/app/api/sessions/[id]/turn/route.ts" | grep -v "^[0-9]*:[[:space:]]*//" | grep -qE "runEncounterOpener" && grep -qE "let openerRan = false" "src/app/api/sessions/[id]/turn/route.ts" && echo WIRED_OK</automated>
  </verify>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -qiE "turn/route" && echo TS_HAS_ERRORS || echo TS_CLEAN</automated>
  </verify>
  <done>route.ts declares let openerRan = false at the vault-branch scope, adds the gated (!isBegin && vaultMutationsEnabled && !encounter.active && detectCombatIntent && !isRollResult) opener hook that awaits getBestiaryStatblock, calls runEncounterOpener, dispatches each event with campaignId AND sessionId, and sets openerRan = true. tsc is clean. No v1/v2 file edited; no damage emitted on the opening turn.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Headless wiring test — goblin intent spawns the REAL goblin hpMax; initiative includes PC UUID + monster id; no real NOTIFY</name>
  <files>tests/app/api/sessions/[id]/turn/encounter-opener-wiring.test.ts</files>
  <read_first>
    - tests/ai/master/vault/projector.test.ts — the fs-seed harness: mkdtempSync(join(tmpdir(), 'gsd-...')) + vi.stubEnv('VAULT_CAMPAIGNS_ROOT', dir) + writeFileSync of a JSONL events.md; afterEach rmSync + vi.unstubAllEnvs + vi.resetModules. Mirror this to seed an encounter-less events.md under a temp campaign dir.
    - tests/app/api/sessions/[id]/turn/combat-resolver.test.ts — the pure-function turn/ test convention (import the function under test directly; deterministic; no live DB).
    - src/app/api/sessions/[id]/turn/encounter-opener.ts (10-01) + src/app/api/sessions/[id]/turn/monster-bestiary.ts (getBestiaryStatblock, 10-02) — the two units this test composes to prove the bestiary path is wired (goblin → hpMax 7), exactly as the route composes them.
    - src/ai/master/vault/tools.ts — dispatchVaultTool persists to events.md and emitStateRefresh is a NO-OP when sessionId is absent (~200-205); the UUID guard at ~288 requires a real-UUID campaignId. So a dispatch WITHOUT sessionId still appends events but fires NO Postgres NOTIFY.
    - src/ai/master/vault/projector.ts — replayEvents / parseEventsFile (~849) to replay the seeded+appended events.md into an EncounterState for the assertions.
  </read_first>
  <behavior>
    - WIRING (the REQ-045 proof, BLOCKER-1 acceptance): compose getBestiaryStatblock + runEncounterOpener exactly as the route does — const stats = await getBestiaryStatblock('goblin'); const events = runEncounterOpener(snap, 'goblin', () => stats); — with a snapshot whose party has one PC UUID. Assert the monster_spawn event's hpMax === 7 (the goblin's REAL committed SRD value, NOT a CR-default). This proves the bestiary path is wired, not just the fallback.
    - INITIATIVE membership: assert the initiative_set.order contains the PC's UUID AND the spawned monster's id, and order.length === party.length + 1.
    - REQ-047: assert NO damage / monster_hp_change / hp_change event appears in the returned events.
    - NO REAL NOTIFY (WARNING-4): drive the dispatch path WITHOUT a sessionId — for each event call dispatchVaultTool('apply_event', ev, { campaignId: <real UUID> }) (campaignId only, no sessionId) against the temp VAULT_CAMPAIGNS_ROOT. Assert each dispatch result is not isError and that events.md gains the combat_start?/monster_spawn/initiative_set sequence (replay → encounter.active === true with the PC UUID + monster id in turnOrder). Because sessionId is absent, emitStateRefresh is a no-op (tools.ts:200) → no Postgres NOTIFY is attempted. SEPARATELY assert (a unit-level expectation, e.g. an inline comment + a string/grep check or a tiny assertion) that PRODUCTION passes sessionId — i.e. document that the route call site includes sessionId (the test does not, to stay headless). [Alternative, if preferred: vi.mock the notify module and assert notifySession was NOT called in this headless path.]
    - CAMPAIGN id: use a real UUID literal for the campaign dir (the dispatchVaultTool UUID guard at tools.ts:288 rejects non-UUIDs).
  </behavior>
  <action>
    Write tests/app/api/sessions/[id]/turn/encounter-opener-wiring.test.ts using the projector.test.ts fs-seed harness (mkdtempSync + vi.stubEnv('VAULT_CAMPAIGNS_ROOT', dir) + writeFileSync a JSONL events.md containing ONLY a campaign_initialized seed — no encounter events). Use a real UUID literal for the campaign id (e.g. "11111111-2222-3333-4444-555555555555") so the dispatchVaultTool UUID guard passes. Build a snapshot fixture whose party is one PC row with a fixed UUID id.

    Compose the two units exactly as the route does: await getBestiaryStatblock('goblin') (reads the committed data/vault/handbook/monsters/goblin.md), then runEncounterOpener(snap, 'goblin', () => stats). Assert: monster_spawn.hpMax === 7 (REAL goblin value — the bestiary-wired acceptance criterion); initiative_set.order contains the PC UUID and the monster id and has length party.length + 1; no damage/monster_hp_change/hp_change event present.

    Then drive the dispatch leg WITHOUT sessionId: for each opener event, await dispatchVaultTool('apply_event', ev, { campaignId }) (NO sessionId — so emitStateRefresh is a no-op and no real Postgres NOTIFY fires). Assert each result.isError is false. Re-read events.md (parseEventsFile + replayEvents) and assert encounter.active === true with both the PC UUID and the monster id in turnOrder. Add an assertion or explicit documented expectation that PRODUCTION (the route) passes sessionId (the headless test omits it on purpose); if you prefer a hard assertion, vi.mock '@/sessions/notify' and assert notifySession was not called on this no-sessionId path.

    Keep the test headless and deterministic — no live Postgres, no HTTP server. Run GREEN. Commit: `test(10): wiring proof — goblin opener uses real SRD hpMax, no real NOTIFY`.
  </action>
  <verify>
    <automated>npx vitest run tests/app/api/sessions/[id]/turn/encounter-opener-wiring.test.ts 2>&1 | grep -qE "passed|PASS" && ! npx vitest run tests/app/api/sessions/[id]/turn/encounter-opener-wiring.test.ts 2>&1 | grep -qE "[1-9][0-9]* failed" && echo WIRING_TEST_OK</automated>
  </verify>
  <done>The wiring test passes: it asserts the goblin spawns with hpMax 7 from the real SRD bestiary (not a CR-default), the initiative order includes the PC UUID + monster id, no damage event is emitted, and the headless dispatch (no sessionId) fires no real NOTIFY while production is documented/asserted to pass sessionId.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM-detected combat intent / monster name → route opener | The player message is untrusted; the route derives a monster name from it and passes it to the opener + bestiary reader (path safety lives in 10-02's safeVaultPath reader). |
| route → events.md via dispatchVaultTool | Opener events are persisted with the SERVER campaign UUID + sessionId; never player-derived. |
| opening turn → damage sequencing | The opening turn must not pre-apply damage (REQ-047). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-07 | Spoofing | campaignId/sessionId in dispatchVaultTool | mitigate | Pass ONLY the server-resolved campaign.id + sessionId (never player-derived), mirroring the v1/v2 emit loops (T-08-06/T-09-18). dispatchVaultTool rejects a non-UUID campaignId (tools.ts:288). |
| T-10-08 | Tampering (sequencing) | damage applied at open | mitigate | The opener emits only monster_spawn/initiative_set (and optional combat_start); the gate is ordered after isRollResult so an opening turn is never a roll-result turn; no monster_hp_change/hp_change is emitted on open (REQ-047). |
| T-10-09 | Denial of Service | opener throwing / hanging the turn | mitigate | The hook is wrapped in try/catch → log + fall through (openerRan stays false); runEncounterOpener never throws; getBestiaryStatblock returns null rather than throwing. |
| T-10-13 | Tampering (regression) | baked / non-combat / begin paths | mitigate | The entire hook is gated on masterBackend==='vault' && vaultMutationsEnabled && combat-intent && !isRollResult && !isBegin; non-combat, baked, roll-result, and begin turns are byte-identical. |
</threat_model>

<verification>
- Task 1 grep + tsc verifies confirm openerRan declared, runEncounterOpener + getBestiaryStatblock wired, tsc clean.
- Task 2 wiring test green: goblin hpMax 7 (real SRD), initiative includes PC UUID + monster id, no damage, headless dispatch fires no real NOTIFY.
- grep route.ts confirms the dispatch passes sessionId in production (call site includes sessionId).
- No edits to combat-resolver / monster-turns / monster-bestiary existing functions / combat-handoff / projector / events-schema.
</verification>

<success_criteria>
- REQ-045 wired end to end: a goblin combat-intent turn opens the encounter with the goblin's REAL SRD hpMax (7) and an initiative_set listing the PC UUID(s) + monster id, dispatched with campaignId AND sessionId.
- REQ-047 held: no damage on the opening turn.
- openerRan boolean planted at the vault-branch scope for 10-04; gate ordered after isRollResult; v1/v2 stack untouched; non-combat/begin/baked turns unchanged.
</success_criteria>

<output>
Create `.planning/phases/10-server-authoritative-combat-and-tracker/10-03-SUMMARY.md` when done.
</output>
