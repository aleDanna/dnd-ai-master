---
phase: 10-server-authoritative-combat-and-tracker
plan: 03
type: execute
wave: 2
depends_on: [10-01, 10-02]
files_modified:
  - src/app/api/sessions/[id]/turn/route.ts
  - tests/app/api/sessions/[id]/turn/encounter-opener-wiring.test.ts
autonomous: true
requirements: [REQ-045, REQ-047]
must_haves:
  truths:
    - "On a vault turn where combat intent is detected and no encounter is active and the result is not a roll, the route calls runEncounterOpener and dispatches its monster_spawn + initiative_set via dispatchVaultTool with BOTH campaignId and sessionId"
    - "The opener trigger is gated by `vaultMutations && !encounter.active && detectCombatIntent && !isRollResult` and is ordered AFTER the isRollResult check"
    - "A 'goblin' combat-intent turn spawns the goblin with its REAL bestiary hpMax (7), not a CR-default — proving getBestiaryStatblock is wired as the opener's bestiaryLookup"
    - "The route never emits a damage event on the opening turn (REQ-047)"
    - "A boolean `openerRan` is declared at the vault-branch scope alongside _resolver/_monsterLoopRan and set true when the opener runs, so the empty-narration branch (10-04) can read it"
  artifacts:
    - path: "src/app/api/sessions/[id]/turn/route.ts"
      provides: "Opener trigger + dispatch wiring; getBestiaryStatblock injected as bestiaryLookup; openerRan cross-plan signal"
      contains: "runEncounterOpener"
    - path: "tests/app/api/sessions/[id]/turn/encounter-opener-wiring.test.ts"
      provides: "Headless wiring test: goblin intent → real goblin hpMax in monster_spawn + initiative order includes PC UUID(s)+monster id; no real Postgres NOTIFY"
      contains: "getBestiaryStatblock"
  key_links:
    - from: "src/app/api/sessions/[id]/turn/route.ts"
      to: "src/app/api/sessions/[id]/turn/encounter-opener.ts (runEncounterOpener)"
      via: "call at the gated combat-intent branch, passing getBestiaryStatblock as bestiaryLookup"
      pattern: "runEncounterOpener"
    - from: "src/app/api/sessions/[id]/turn/route.ts"
      to: "src/app/api/sessions/[id]/turn/monster-bestiary.ts (getBestiaryStatblock)"
      via: "injected as the opener's bestiaryLookup"
      pattern: "getBestiaryStatblock"
    - from: "src/app/api/sessions/[id]/turn/route.ts"
      to: "src/ai/master/vault/tools.ts (dispatchVaultTool)"
      via: "dispatch monster_spawn + initiative_set with campaignId AND sessionId"
      pattern: "dispatchVaultTool"
---

<objective>
Wire the server-authoritative opener into the live turn route. When a vault turn detects combat intent, no encounter is active, and the turn is not a roll result, the route calls `runEncounterOpener` (10-01) with `getBestiaryStatblock` (10-02) injected as the `bestiaryLookup`, then dispatches the returned `monster_spawn` + `initiative_set` through `dispatchVaultTool` with BOTH campaignId and sessionId. This completes REQ-045 end to end (server selects the monster from the SRD bestiary and rolls initiative) and holds the REQ-047 invariant (no damage on the opener turn).

It also plants the cross-plan signal `openerRan` (a boolean at the vault-branch scope, alongside `_resolver`/`_monsterLoopRan`) that 10-04's empty-narration `notifySession` guard reads.

Purpose: Turn the tested pure pieces into a working in-game encounter open, proving the REAL bestiary path is wired (goblin spawns at hpMax 7, not a CR guess).
Output: route wiring + a headless integration test that fires no real Postgres NOTIFY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-server-authoritative-combat-and-tracker/10-CONTEXT.md
@.planning/phases/10-server-authoritative-combat-and-tracker/10-RESEARCH.md
@src/app/api/sessions/[id]/turn/route.ts
@src/app/api/sessions/[id]/turn/encounter-opener.ts
@src/app/api/sessions/[id]/turn/monster-bestiary.ts
@src/ai/master/vault/tools.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire the gated opener trigger + dispatch + openerRan signal into route.ts</name>
  <files>src/app/api/sessions/[id]/turn/route.ts</files>
  <read_first>
    - src/app/api/sessions/[id]/turn/route.ts — CONFIRMED anchors: `_playerMessage` declared ~316-318, `_resolver` declared ~337, `_monsterLoopRan` declared ~399, the empty-narration `else` branch ~748-755 (10-04 territory). The isRollResult check the trigger must follow, and the existing vault-branch where `vaultMutations`/`encounter`/`detectCombatIntent` are in scope.
    - src/app/api/sessions/[id]/turn/encounter-opener.ts — runEncounterOpener(snapshot, monsterName, bestiaryLookup) signature from 10-01.
    - src/app/api/sessions/[id]/turn/monster-bestiary.ts — getBestiaryStatblock(name) from 10-02; pass it directly as bestiaryLookup.
    - src/ai/master/vault/tools.ts — dispatchVaultTool signature and that it must receive campaignId AND sessionId in production; UUID guard at ~288 (campaignId must be a real UUID); emitStateRefresh is a no-op when sessionId is absent (~201).
    - .planning/phases/10-server-authoritative-combat-and-tracker/10-CONTEXT.md — locked: Option B only (no constrained-JSON/Ollama `format` opener — deferred; keep monster-selection swappable for future option A); monster_spawn {id,name,hpMax} + initiative_set order (PC UUID(s)+monster id); dispatch with campaignId AND sessionId; REQ-047 no damage on opener.
  </read_first>
  <action>
    Declare `let openerRan = false;` at the SAME vault-branch scope where `_resolver` (~337) and `_monsterLoopRan` (~399) are declared — it must be a boolean (to match 10-04's `|| openerRan` guard) and visible to the empty-narration branch ~390 lines below (~748). State its name/scope explicitly so 10-04 binds to the same identifier.

    Add the opener trigger AFTER the isRollResult check, gated by exactly: `vaultMutations && !encounter.active && detectCombatIntent && !isRollResult`. Inside the gate:
    1. Resolve the monster name from the existing combat-intent detection (the name detectCombatIntent surfaces; do not invent a new detector — Option B, no constrained JSON).
    2. Call `runEncounterOpener(snap, monsterName, getBestiaryStatblock)` — inject getBestiaryStatblock as the bestiaryLookup so real SRD stats are used (the goblin gets hpMax 7), with the opener's own null-fallback covering unknown monsters.
    3. For each returned event, dispatch via `dispatchVaultTool` passing BOTH campaignId (the real campaign UUID) AND sessionId (so emitStateRefresh fires in production and the tracker updates). monster_spawn carries {id,name,hpMax}; initiative_set.order includes the PC UUID(s) + the monster id.
    4. Set `openerRan = true;` after a successful opener run.
    Add a one-line code comment noting per INFO-9 that PC rows carry no initiativeBonus/initiative field (characters schema has ac/hpMax only), so PC initiative is 1d20+0 by design inside the opener — this is intentional, not dropped wiring.
    Do NOT emit any damage on this turn (REQ-047). Do NOT edit v1/v2 files (combat-resolver/monster-turns/monster-bestiary math/combat-handoff/projector/events-schema). Do NOT add a constrained-JSON/Ollama `format` path. Keep monster-selection swappable (the route passes a name + injected lookup; the future option-A selector replaces only the name-resolution, not the opener).
  </action>
  <verify>
    <automated>grep -n "openerRan\|runEncounterOpener\|getBestiaryStatblock\|dispatchVaultTool" "src/app/api/sessions/[id]/turn/route.ts" | grep -v '^[0-9]*:[[:space:]]*//' | grep -qE "runEncounterOpener" &amp;&amp; npx tsc --noEmit -p tsconfig.json 2>&1 | grep -qiE "turn/route" &amp;&amp; echo TS_HAS_ERRORS || echo WIRED_OK