---
phase: 07-vault-combat-playable-d2
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ai/master/vault/tools.ts
  - data/vault/tools/apply_event.md
  - data/vault/tools/index.md
  - scripts/seed-bestiary.ts
  - data/vault/handbook/monsters/*.md
autonomous: true
requirements: [REQ-038]

must_haves:
  truths:
    - "apply_event dispatches combat_start / monster_spawn / initiative_set / turn_advance / monster_hp_change / combat_end without rejecting due to missing payload.character"
    - "Non-encounter events (e.g. hp_change without payload.character) are still rejected by the UUID guard"
    - "apply_event input_schema description string lists all 6 encounter type names and their payload shapes"
    - "data/vault/handbook/monsters/ contains exactly 180 .md files after seed script runs"
    - "goblin.md frontmatter: hpMax:7, ac:15, initiativeBonus:2"
    - "data/vault/tools/apply_event.md exists with lifecycle summary and the 6 encounter event shapes"
    - "data/vault/tools/index.md has an apply_event entry (4 tools total)"
  artifacts:
    - path: "src/ai/master/vault/tools.ts"
      provides: "UUID-guard relaxed for ENCOUNTER_EVENT_TYPES; apply_event description extended"
      contains: "ENCOUNTER_EVENT_TYPES.has"
    - path: "scripts/seed-bestiary.ts"
      provides: "Reads data/monsters.csv, writes 180 data/vault/handbook/monsters/<slug>.md files"
      min_lines: 30
    - path: "data/vault/handbook/monsters/goblin.md"
      provides: "Spot-check monster file"
      contains: "hpMax"
    - path: "data/vault/tools/apply_event.md"
      provides: "Per-tool doc with encounter event shapes"
      contains: "combat_start"
    - path: "data/vault/tools/index.md"
      provides: "Index now lists apply_event"
      contains: "apply_event"
  key_links:
    - from: "tools.ts UUID guard (line ~283)"
      to: "ENCOUNTER_EVENT_TYPES import"
      via: "ENCOUNTER_EVENT_TYPES.has(type) skip condition"
      pattern: "ENCOUNTER_EVENT_TYPES\\.has"
    - from: "scripts/seed-bestiary.ts"
      to: "data/vault/handbook/monsters/"
      via: "fs.writeFile per row"
      pattern: "writeFile.*monsters"
---

<objective>
Unlock the D1 encounter events for LLM use (tool exposure) and seed the 180-monster SRD bestiary.

Purpose: The D1 pipeline already validates, persists, and regenerates views for the 6 encounter event types. The only blocker keeping the LLM from emitting them is the UUID guard in tools.ts and the absence of those types in the apply_event schema description. This plan removes both blockers and builds the monster reference library the master will read.

Output: Modified tools.ts (guard skip + description extension), new apply_event.md doc, updated index.md, new seed script, 180 committed bestiary files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-vault-combat-playable-d2/07-CONTEXT.md
@.planning/phases/06-vault-combat-state-foundation-d1/06-01-SUMMARY.md
@src/ai/master/vault/tools.ts
@src/ai/master/vault/events-schema.ts
@data/vault/tools/index.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Relax UUID guard for encounter events + extend apply_event description + add tool docs</name>
  <files>src/ai/master/vault/tools.ts, data/vault/tools/apply_event.md, data/vault/tools/index.md, tests/ai/master/vault/tools.test.ts</files>
  <read_first>
    - src/ai/master/vault/tools.ts lines 87-106 — the apply_event tool entry: the description string (line ~89) and the input_schema description strings for type (~line 96) and payload (~lines 100-101). These are the strings to extend.
    - src/ai/master/vault/tools.ts lines 275-291 — the UUID guard block: the if-condition at line 283 checks `guarded.value.type !== 'campaign_initialized'` before rejecting a missing payload.character. This is the only blocker for encounter events.
    - src/ai/master/vault/events-schema.ts — ENCOUNTER_EVENT_TYPES export (Set of 6 strings: combat_start, monster_spawn, initiative_set, turn_advance, monster_hp_change, combat_end). Payload shapes are in the discriminated union. Import this set into tools.ts.
    - data/vault/tools/index.md — current 3-tool index format; apply_event entry must be added in the same style as the existing entries.
    - data/vault/tools/read_vault_multi.md or list_vault.md — read one file to confirm the per-tool doc format before writing apply_event.md.
    - tests/ai/master/vault/tools.test.ts — existing test structure (dispatchVaultTool test suite). Locate the apply_event tests to understand where to add new assertions.
    - .planning/phases/07-vault-combat-playable-d2/07-CONTEXT.md — §"Tool exposure (LOCKED)" for the exact guard condition and description extension scope.
  </read_first>
  <behavior>
    New describe block in tools.test.ts: "apply_event — encounter event dispatch (D2 UUID guard skip)":

    Test (a): combat_start dispatches without payload.character.
      Input: type='combat_start', payload={}, valid campaignId, a real tmpdir events.md.
      Assertion: result.isError is falsy (or absent); result.content does NOT start with 'ERROR'.

    Test (b): monster_spawn dispatches without payload.character.
      Input: type='monster_spawn', payload={id:'goblin-1', name:'Goblin', hpMax:7, ac:15, initiativeBonus:2}.
      Assertion: result.isError is falsy.

    Test (c): a character event (hp_change) without payload.character is STILL rejected.
      Input: type='hp_change', payload={delta:-5} (no character field).
      Assertion: result.isError is true AND result.content contains 'UUID'.

    Test (d): the apply_event tool description string contains all 6 encounter type names.
      Import VAULT_TOOL_DEFINITIONS. Find the apply_event entry. Read its input_schema.properties.type.description.
      Assertions: each of ['combat_start','monster_spawn','initiative_set','turn_advance','monster_hp_change','combat_end'] is a substring of that description string.
  </behavior>
  <action>
    RED sub-step: Add the new describe block to tests/ai/master/vault/tools.test.ts with all four assertions. Run the suite — (a), (b) fail (UUID error returned); (c) passes; (d) fails (names absent). This is the expected RED state.

    GREEN sub-step — three independent changes to tools.ts:

    1. Import ENCOUNTER_EVENT_TYPES at the top of tools.ts, alongside the existing VAULT_EVENT_TYPES / validateEvent imports from events-schema.ts.

    2. UUID guard relaxation (line ~283): change the guard condition from:
         `if (guarded.value.type !== 'campaign_initialized') {`
       to:
         `if (guarded.value.type !== 'campaign_initialized' && !ENCOUNTER_EVENT_TYPES.has(guarded.value.type)) {`
       This skips the payload.character UUID check for all 6 encounter event types while keeping the guard active for all 28 character-scoped events. The rest of the guard body is unchanged.

    3. Extend the apply_event tool description and schema description strings:
       - The top-level tool description (line ~89) currently says "Append a game-state mutation event (HP, conditions, slots, inventory...)". Append to it: ". Also drives combat encounters: combat_start, monster_spawn, initiative_set, turn_advance, monster_hp_change, combat_end (these have no payload.character — see payload description)."
       - The input_schema.properties.type.description string (line ~96): append the 6 encounter type names to the "One of:" list: ", combat_start, monster_spawn, initiative_set, turn_advance, monster_hp_change, combat_end."
       - The input_schema.properties.payload.description string (lines ~100-101): append after the existing Phase 03 entries: " Encounter events (no payload.character): combat_start {}; monster_spawn {id:string, name:string, hpMax:number, ac?:number, initiativeBonus?:number}; initiative_set {order:[{actorId:string, initiative:number}]}; turn_advance {}; monster_hp_change {id:string, delta:number}; combat_end {}."

    Write data/vault/tools/apply_event.md with this content (each section on its own line in the file):
    - A front-matter section with name: apply_event and the tool purpose.
    - A section "## Encounter lifecycle" listing the 6 event types with payload shapes (matching the D1 LOCKED event table from 07-CONTEXT.md §"Event schema"). Note that encounter events have no payload.character.
    - A section "## Character events" briefly noting that all other events require payload.character to be the character UUID from the materialized view frontmatter.
    - A section "## Combat tracker" noting the live tracker is at campaigns/<id>/combat.md.

    Update data/vault/tools/index.md: add an apply_event entry. Read the current file first to match the existing entry format exactly (tool name, one-line purpose, link to the per-tool doc).

    Run pnpm vitest run tests/ai/master/vault/tools.test.ts -- all new tests pass.
    Run pnpm tsc --noEmit -- exits 0.
  </action>
  <verify>
    <automated>pnpm vitest run tests/ai/master/vault/tools.test.ts &amp;&amp; pnpm tsc --noEmit</automated>
  </verify>
  <done>
    UUID guard skips encounter events; character events still rejected without UUID; all 6 encounter type names in the schema description; apply_event.md created; index.md updated; typecheck clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Seed 180-monster SRD bestiary from data/monsters.csv</name>
  <files>scripts/seed-bestiary.ts, data/vault/handbook/monsters/*.md</files>
  <read_first>
    - data/monsters.csv — first 3 rows (run: head -4 data/monsters.csv). Columns: name,size,type,alignment,armor_class,hit_points,speed,str,dex,con,int,wis,cha,saving_throws,skills,damage_resistances,damage_immunities,condition_immunities,senses,languages,challenge_rating,xp,traits,actions,source. The slug is derived from the name: lowercase, spaces to hyphens, strip special chars (e.g. "Ape" → "ape", "Awakened Shrub" → "awakened-shrub").
    - src/db/schema/srd-monster.ts — confirm the column names match the CSV headers (the SUMMARY says this is the source schema). Check the hp and armor_class column shapes — hp is "7 (2d6)" format; armor_class is "15" or "15 (natural armor)" format. The leading int is extracted by parseInt() for hpMax and ac.
    - scripts/_set-campaign-model.ts — script structure to follow: import ./_env-loader only if needed (this script is read-only from CSV + filesystem, no DB access needed); use process.argv for any flags; close resources when done.
    - .planning/phases/07-vault-combat-playable-d2/07-CONTEXT.md — §"Bestiary (LOCKED)" for the exact 4 frontmatter keys (name, hpMax, ac, initiativeBonus) + cr/xp; initiativeBonus = floor((dex-10)/2).
  </read_first>
  <action>
    Write scripts/seed-bestiary.ts. The script has no dependencies beyond Node.js built-ins (fs, path) and a CSV parser — use the built-in approach: split on newlines, parse each line by splitting on the FIRST comma of each field (the CSV uses double-quoted fields for columns that contain commas; use a simple quoted-CSV parser or the existing pattern from the codebase if one exists — grep for csv parsing in scripts/ first).

    Script logic:
    1. Read data/monsters.csv as a UTF-8 string.
    2. Parse header row to get column indices for: name, armor_class, hit_points, dex, challenge_rating, xp, traits, actions, source.
    3. For each data row (181 rows total including header → 180 data rows):
       a. Derive slug: name.toLowerCase().replace(/[^a-z0-9]+/g, '-').replace(/^-|-$/g, '').
       b. hpMax = parseInt(hit_points) — parseInt handles "7 (2d6)" → 7.
       c. ac = parseInt(armor_class) — parseInt handles "15 (natural armor)" → 15.
       d. dexInt = parseInt(dex).
       e. initiativeBonus = Math.floor((dexInt - 10) / 2).
       f. cr = challenge_rating (string as-is, e.g. "1/2", "1", "20").
       g. xpVal = xp (string as-is).
       h. Build the markdown file content:
          ```
          ---
          name: <name>
          hpMax: <hpMax>
          ac: <ac>
          initiativeBonus: <initiativeBonus>
          cr: "<cr>"
          xp: <xpVal>
          ---

          ## Actions

          <actions text>

          ## Traits

          <traits text (may be empty)>
          ```
          Each section is present only if the field is non-empty (omit ## Traits if traits is blank).
       i. Ensure the output directory data/vault/handbook/monsters/ exists (mkdir -p).
       j. Write data/vault/handbook/monsters/<slug>.md.

    4. Print a summary: "Seeded N monsters to data/vault/handbook/monsters/".

    After writing the script, run it:
      pnpm tsx scripts/seed-bestiary.ts

    Verify the output and then commit the 180 generated files to git along with the seed script. The bestiary is static committed data — these files belong in the repo.

    Do NOT skip or defer the git commit of the generated files — the operator smoke (Plan 04) and the master's monster-stats rule both depend on the files being present in the filesystem.
  </action>
  <verify>
    <automated>ls data/vault/handbook/monsters/ | wc -l</automated>
  </verify>
  <done>
    180 .md files present in data/vault/handbook/monsters/; goblin.md frontmatter has hpMax:7, ac:15, initiativeBonus:2; scripts/seed-bestiary.ts committed; all 180 generated files committed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM → apply_event dispatcher | The LLM now emits encounter events. These are validated by the existing validateEvent gate before the UUID-guard runs. The guard relaxation is scoped only to ENCOUNTER_EVENT_TYPES — character events still require a valid UUID. |
| CSV → filesystem | seed-bestiary.ts reads a committed CSV and writes to a committed output directory. No network, no user input, no PII. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | UUID guard relaxation in tools.ts | mitigate | Relaxation is gated on ENCOUNTER_EVENT_TYPES.has(type) — an O(1) membership check against the 6 known encounter types. Character events retain the UUID validation. The test suite verifies hp_change without character is still rejected. |
| T-07-02 | Tampering | monster id in monster_spawn/monster_hp_change | accept | Monster ids are master-invented free strings that index encounter state only. They have no PII, no auth link, no injection sink. The projector uses them as dictionary keys for encounter-local monster state. |
| T-07-03 | Information Disclosure | bestiary files (committed static data) | accept | SRD monster data is open-licensed game content. No PII, no secrets. Committed to the repo like other static vault knowledge. |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No new packages in this plan. seed-bestiary.ts uses only Node.js built-ins (fs, path). |
</threat_model>

<verification>
- pnpm vitest run tests/ai/master/vault/tools.test.ts exits 0 (including new D2 encounter-dispatch assertions).
- pnpm tsc --noEmit exits 0.
- grep -c 'ENCOUNTER_EVENT_TYPES.has' src/ai/master/vault/tools.ts returns >= 1.
- grep -E 'combat_start|monster_spawn|initiative_set|turn_advance|monster_hp_change|combat_end' src/ai/master/vault/tools.ts confirms all 6 type names in the description strings.
- ls data/vault/handbook/monsters/ | wc -l returns 180.
- grep 'hpMax' data/vault/handbook/monsters/goblin.md returns a line with value 7.
- grep 'apply_event' data/vault/tools/index.md returns at least 1 hit.
</verification>

<success_criteria>
- [ ] ENCOUNTER_EVENT_TYPES imported and used in the UUID guard condition in tools.ts.
- [ ] apply_event dispatches combat_start, monster_spawn, initiative_set, turn_advance, monster_hp_change, combat_end without UUID rejection.
- [ ] hp_change without payload.character is still rejected.
- [ ] All 6 encounter type names appear in the apply_event input_schema type.description string.
- [ ] Encounter payload shapes appear in the input_schema payload.description string.
- [ ] data/vault/tools/apply_event.md created with encounter lifecycle + payload table.
- [ ] data/vault/tools/index.md updated with apply_event entry.
- [ ] 180 monster files in data/vault/handbook/monsters/.
- [ ] goblin.md: hpMax:7, ac:15, initiativeBonus:2.
- [ ] scripts/seed-bestiary.ts committed; 180 generated files committed.
- [ ] pnpm tsc --noEmit exits 0.
</success_criteria>

<output>
Create `.planning/phases/07-vault-combat-playable-d2/07-01-SUMMARY.md` when done.
</output>
