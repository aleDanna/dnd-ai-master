---
phase: 07-vault-combat-playable-d2
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ai/master/vault/prompt-builder.ts
  - tests/ai/master/vault/prompt-builder.test.ts
autonomous: true
requirements: [REQ-038, REQ-022]

must_haves:
  truths:
    - "buildVaultSystemPrompt emits a Combat-lifecycle block when and only when vaultMutations === true"
    - "Block contains all 6 event type names (combat_start through combat_end) as the lifecycle sequence"
    - "Block contains the monster-stats rule: standard → read handbook/monsters/<slug>.md; custom boss → invent + inline"
    - "Block contains the turn rule: run monster turns, stop on PC turn, never act for the PC"
    - "REQ-022 preserved: read-only baseline hash (vaultMutations undefined, toolCount:3) is unchanged"
    - "1000-build stability holds for vaultMutations:true input combinations"
    - "Block is absent when vaultMutations is false or undefined (byte-stable default)"
  artifacts:
    - path: "src/ai/master/vault/prompt-builder.ts"
      provides: "vaultMutations-gated Combat-lifecycle block; VaultPromptInput unchanged (vaultMutations already exists)"
      contains: "combat_start"
    - path: "tests/ai/master/vault/prompt-builder.test.ts"
      provides: "Combat-lifecycle content + gating + REQ-022 stability assertions"
      contains: "combat_start"
  key_links:
    - from: "VaultPromptInput.vaultMutations"
      to: "Combat-lifecycle block emission"
      via: "if (input.vaultMutations === true) lines.push(...combatLifecycleBlock(...))"
      pattern: "vaultMutations.*true"
    - from: "Combat-lifecycle block"
      to: "REQ-022 __forbidden-patterns.ts"
      via: "no Date.now / Math.random / process.env in new block lines"
      pattern: "combat_start.*combat_end"
---

<objective>
Add the Combat-lifecycle prompt block to buildVaultSystemPrompt, gated on vaultMutations === true, REQ-022 byte-stable.

Purpose: The master needs explicit instruction on how to drive combat using the D1 encounter events (now unlocked by Plan 01). Without this block the model has no knowledge of the lifecycle sequence, monster-stats rule, or turn rule. This block is the behavioral specification for LLM-driven combat.

Output: Modified prompt-builder.ts (new gated block inserted after applyEventMention), extended prompt-builder.test.ts (content/gating/stability assertions). The existing locked-snapshot hash (60e567...) must remain UNCHANGED — the block is only emitted when vaultMutations is true, so the read-only baseline is unaffected.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-vault-combat-playable-d2/07-CONTEXT.md
@src/ai/master/vault/prompt-builder.ts
@src/ai/master/vault/__forbidden-patterns.ts
@tests/ai/master/vault/prompt-builder.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED tests then GREEN — Combat-lifecycle block in buildVaultSystemPrompt</name>
  <files>src/ai/master/vault/prompt-builder.ts, tests/ai/master/vault/prompt-builder.test.ts</files>
  <read_first>
    - src/ai/master/vault/prompt-builder.ts lines 197-295 — the full body of buildVaultSystemPrompt from applyEventMention construction through the return statement. Key landmarks:
        line ~197: applyEventMention conditional.
        line ~252: if (applyEventMention.length > 0) lines.push(applyEventMention) — this is the insertion point; the Combat-lifecycle block goes immediately AFTER this block (still gated on vaultMutations === true).
        line ~256: the character roster block.
        line ~276: the manualRolls block.
        line ~288: the language clause.
      Insert the new block AFTER the applyEventMention push (line ~254) and BEFORE the character roster block (line ~256). The block is gated independently on vaultMutations === true, same as applyEventMention.
    - src/ai/master/vault/__forbidden-patterns.ts — the forbidden regexes (Date.now, Math.random, process.env, randomUUID, process.hrtime, new Date). No new block line may match these.
    - tests/ai/master/vault/prompt-builder.test.ts lines 1-100 — BASE_INPUT constant (vaultRoot, campaignId, toolCount:3, vaultMutations unset), the locked-snapshot hash test (line ~80, hash 60e56767b9c63ae936741fc6812a3958c6be346662736a455bed75510c54b14e), the REQ-022 stability describe block. Do NOT modify these.
    - .planning/phases/07-vault-combat-playable-d2/07-CONTEXT.md — §"Combat-lifecycle prompt block (LOCKED)" for the three semantic sections: lifecycle sequence, monster-stats rule, turn rule. Wording is at implementer discretion; semantics are locked.
  </read_first>
  <behavior>
    New describe block in prompt-builder.test.ts: "buildVaultSystemPrompt — Phase 07 Combat-lifecycle block (REQ-038)":

    Test (a): vaultMutations:false (default, BASE_INPUT) → block ABSENT.
      Assertion: !prompt.includes('combat_start').

    Test (b): vaultMutations:true, toolCount:4 → block PRESENT with lifecycle event names.
      Input: {...BASE_INPUT, vaultMutations:true, toolCount:4}.
      Assertions (all on same prompt):
        - prompt.includes('combat_start')
        - prompt.includes('monster_spawn')
        - prompt.includes('initiative_set')
        - prompt.includes('turn_advance')
        - prompt.includes('monster_hp_change')
        - prompt.includes('combat_end')

    Test (c): vaultMutations:true → block contains monster-stats rule tokens.
      Input: same as (b).
      Assertions:
        - prompt includes 'handbook/monsters/' (the path the master is told to read)
        - prompt includes some token indicating custom/boss monsters should have stats inline in monster_spawn (e.g. 'monster_spawn' appears in the block in the context of "invent" or "inline" — pick the token you write)

    Test (d): vaultMutations:true → block contains turn rule tokens.
      Input: same as (b).
      Assertions:
        - prompt includes some form of "stop" related to PC turns (the turn rule says stop when it becomes a PC's turn)
        - prompt does NOT instruct the master to act for the PC (verify 'do not act for the PC' or equivalent token is present)

    Test (e): REQ-022 — 1000-build stability for vaultMutations:true.
      Input: {...BASE_INPUT, vaultMutations:true, toolCount:4}.
      Assertion: all 1000 hashes equal hashes[0] (Set.size === 1).

    Test (f): REQ-022 — existing locked-snapshot hash unchanged.
      This test already exists in the file. Verify it still passes UNCHANGED — the new block must NOT emit for BASE_INPUT (vaultMutations undefined). No code change needed; this is a guard that the new block is truly gated.

    NOTE: Keep the test for the existing locked-snapshot test verbatim. Do not alter the hash value 60e56767b9c63ae936741fc6812a3958c6be346662736a455bed75510c54b14e.
  </behavior>
  <action>
    TDD RED then GREEN. Single file pair.

    RED sub-step: Add the new describe block to tests/ai/master/vault/prompt-builder.test.ts. Run the suite — test (a) passes (block absent); tests (b)-(e) fail; (f) passes (hash unchanged). This RED state confirms the tests drive the implementation.

    GREEN sub-step — add the Combat-lifecycle block to src/ai/master/vault/prompt-builder.ts:

    1. Write a module-level helper (or inline array) that returns the Combat-lifecycle block lines. The helper takes no arguments (it is deterministic — no inputs vary per call; the block content is static). Example shape: function combatLifecycleBlock(): string[] { return [ '## Combat lifecycle', '', ... ]; }

    2. The block content (semantics locked; wording at implementer discretion) must cover exactly three semantic areas:

       Area A — Lifecycle sequence. Instruct the master to drive combat using these events in order:
         - combat_start: opens the encounter (call once at the start of a fight)
         - monster_spawn: one call per enemy (include name, hpMax, ac, initiativeBonus from handbook/monsters/<slug>.md or invented for custom bosses)
         - initiative_set: pass the full ordered list of all actors (PC UUIDs + monster ids with their initiative values)
         - Per-turn: monster_hp_change when a monster takes damage, and turn_advance to end each actor's turn
         - combat_end: closes the encounter when the fight ends
         - Mention that the live tracker is at campaigns/<id>/combat.md

       Area B — Monster-stats rule:
         - For a standard SRD creature: read handbook/monsters/<slug>.md and copy name, hpMax, ac, initiativeBonus into the monster_spawn payload.
         - For a campaign-specific boss not in the bestiary: invent appropriate stats and put them in the monster_spawn payload.

       Area C — Turn rule:
         - On a monster's turn: narrate its action, apply effects (monster_hp_change for monster damage taken; hp_change on the target PC for PC damage taken), then call turn_advance.
         - Run through consecutive monster turns automatically.
         - Stop when it becomes a PC's turn — let that player act. Do NOT act for the PC (Phase 04 anti-railroading rule holds). PC attack/damage rolls use the ## Rolls surface (Phase 05).

    3. Each physical line of the block must be a separate explicit string element pushed to the lines array — no multi-line template literals (same paranoia as the existing blocks; confirmed by reading prompt-builder.ts comments). No Date.now, Math.random, process.env, randomUUID, or new Date in the block.

    4. Insert the gated block into buildVaultSystemPrompt immediately after the applyEventMention push block (after the lines.push('') that follows applyEventMention, before the character roster if-block):

       if (input.vaultMutations === true) {
         for (const line of combatLifecycleBlock()) {
           lines.push(line);
         }
         lines.push('');
       }

    Run pnpm vitest run tests/ai/master/vault/prompt-builder.test.ts -- all tests pass including the existing locked-snapshot hash.
    Run pnpm tsc --noEmit -- exits 0.
    Run grep -nE 'Date\.now|Math\.random|process\.env|new Date|randomUUID|process\.hrtime' src/ai/master/vault/prompt-builder.ts -- no new matches in the combat lifecycle block lines.
  </action>
  <verify>
    <automated>pnpm vitest run tests/ai/master/vault/prompt-builder.test.ts &amp;&amp; pnpm tsc --noEmit</automated>
  </verify>
  <done>
    Combat-lifecycle block emitted when vaultMutations:true; absent when false/undefined; block contains all 6 event type names + monster-stats rule + turn rule; existing locked-snapshot hash 60e56767b9c63ae936741fc6812a3958c6be346662736a455bed75510c54b14e unchanged; 1000-build stability for vaultMutations:true; no forbidden patterns; typecheck clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → buildVaultSystemPrompt | vaultMutations is a boolean resolved server-side from campaign settings; no LLM or user string is injected into the block content. The block is static/deterministic. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | Tampering | Combat-lifecycle block content (REQ-022) | mitigate | Block is a static string array with no runtime inputs. __forbidden-patterns.ts ESLint rule + test enforcement ensures no Date.now/Math.random/process.env enters the builder. The 1000-build stability test catches any non-determinism. |
| T-07-02-02 | Repudiation | LLM combat lifecycle deviation | accept | Model obedience to the lifecycle sequence is a behavioral property, not a code-level security threat. Verified in the operator smoke (Plan 04), not unit-tested. |
| T-07-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages. |
</threat_model>

<verification>
- pnpm vitest run tests/ai/master/vault/prompt-builder.test.ts exits 0 (all new Phase 07 assertions + existing locked-snapshot hash 60e56767b9c63ae936741fc6812a3958c6be346662736a455bed75510c54b14e + existing REQ-022 1000-build tests all pass).
- pnpm tsc --noEmit exits 0.
- grep -nE 'Date\.now|Math\.random|process\.env|new Date|randomUUID|process\.hrtime' src/ai/master/vault/prompt-builder.ts returns no matches in the new block lines.
- grep -c 'combat_start' src/ai/master/vault/prompt-builder.ts returns >= 1.
- grep 'handbook/monsters/' src/ai/master/vault/prompt-builder.ts returns at least 1 hit in the new block.
</verification>

<success_criteria>
- [ ] Combat-lifecycle block emitted in buildVaultSystemPrompt when vaultMutations===true.
- [ ] Block absent when vaultMutations is false or undefined.
- [ ] Block contains all 6 encounter event type names in lifecycle order.
- [ ] Block contains monster-stats rule: read handbook/monsters/<slug>.md for standard; invent + inline for custom boss.
- [ ] Block contains turn rule: run monster turns, stop on PC turn, do not act for the PC.
- [ ] REQ-022: read-only baseline hash 60e56767b9c63ae936741fc6812a3958c6be346662736a455bed75510c54b14e UNCHANGED.
- [ ] REQ-022: 1000-build stability for vaultMutations:true input.
- [ ] No forbidden patterns (Date.now / Math.random / process.env / new Date / randomUUID).
- [ ] pnpm tsc --noEmit exits 0.
</success_criteria>

<output>
Create `.planning/phases/07-vault-combat-playable-d2/07-02-SUMMARY.md` when done.
</output>
