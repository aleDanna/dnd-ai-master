---
phase: 07-vault-combat-playable-d2
plan: "04"
type: execute
wave: 3
depends_on: ["07-01", "07-02", "07-03"]
files_modified: []
autonomous: false
requirements: [REQ-038]

must_haves:
  truths:
    - "One Piece campaign has settings.sourceOfTruth='vault' (data change committed)"
    - "The PC mechanics pane (HP, conditions) renders from vault characters/<slug>.md"
    - "The CombatTracker renders from vault combat.md when a fight is active"
    - "Master spawns a monster, sets initiative, turns alternate: PC acts on their turn, master runs monster turns"
    - "HP changes land in the tracker (monster_hp_change events reflected)"
    - "combat_end clears the tracker"
  artifacts:
    - path: "scripts/_set-source-of-truth.ts"
      provides: "Operator script to flip sourceOfTruth on a campaign; pattern from _set-campaign-model.ts"
      contains: "sourceOfTruth"
  key_links:
    - from: "campaigns.settings.sourceOfTruth"
      to: "buildClientSnapshot vault path"
      via: "sourceOfTruth === 'vault' gate in client-snapshot.ts"
      pattern: "sourceOfTruth.*vault"
---

<objective>
Flip One Piece to sourceOfTruth='vault' and run the operator smoke that validates all five D2 pieces together.

Purpose: This is the live integration smoke: it proves the full D2 stack (tool exposure, prompt block, bestiary, turn interleaving) works end-to-end on a real gemma4 session. It also serves as the operator checkpoint that the CombatTracker renders vault state (D1 06-02 prerequisite: sourceOfTruth='vault' must be set for the live UI to read from vault instead of Postgres).

Output: One Piece campaign with sourceOfTruth='vault' (persisted data change); operator-confirmed fight that plays through to combat_end. This plan is autonomous:false — it requires the operator to run the fight and confirm.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-vault-combat-playable-d2/07-CONTEXT.md
@scripts/_set-campaign-model.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write _set-source-of-truth.ts script and flip One Piece</name>
  <files>scripts/_set-source-of-truth.ts</files>
  <read_first>
    - scripts/_set-campaign-model.ts — the full file. This is the pattern to follow exactly: import ./_env-loader, parse a campaign-id prefix from argv, SELECT the campaign row, build a new settings object, UPDATE, log the change, pool.end(). The only difference is the field being set: sourceOfTruth instead of aiMasterModel.
    - src/db/schema/index.ts or src/db/schema/campaigns.ts — confirm the settings JSONB column name and the drizzle update syntax (already used in _set-campaign-model.ts so the import paths are confirmed there).
  </read_first>
  <action>
    Write scripts/_set-source-of-truth.ts following the exact same structure as _set-campaign-model.ts:

    - Usage: pnpm tsx scripts/_set-source-of-truth.ts &lt;campaign-id-prefix&gt; &lt;vault|postgres&gt;
    - Parse [prefix, value] from process.argv.slice(2).
    - Validate value is 'vault' or 'postgres'; exit 2 otherwise.
    - SELECT the campaign row by prefix (same ambiguity guard as _set-campaign-model.ts).
    - Build next settings: { ...row.settings, sourceOfTruth: value }.
    - UPDATE campaigns SET settings = next, updated_at = now() WHERE id = row.id.
    - Log: "✓ &lt;name&gt; (&lt;id prefix&gt;) sourceOfTruth: &lt;prev&gt; → &lt;value&gt;".
    - pool.end().

    Then run the script to flip the One Piece campaign. Find the One Piece campaign id prefix by running:
      pnpm tsx -e "import './_env-loader'; const {db} = await import('@/db/client'); const {campaigns} = await import('@/db/schema'); const rows = await db.select({id:campaigns.id,name:campaigns.name}).from(campaigns); console.log(rows.map(r=>r.id.slice(0,8)+' '+r.name).join('\n')); process.exit(0);"
    (or use the known prefix if it is in the codebase — check scripts/ for any hardcoded One Piece id reference, or check .env.local for ONE_PIECE_CAMPAIGN_ID).

    After identifying the prefix, run:
      pnpm tsx scripts/_set-source-of-truth.ts &lt;one-piece-prefix&gt; vault

    Verify the update logged correctly.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit</automated>
  </verify>
  <done>
    _set-source-of-truth.ts committed; One Piece campaign has sourceOfTruth='vault' in its settings JSONB (confirmed by the script log output).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator smoke — play a fight on One Piece (gemma4) and verify D2 end-to-end</name>
  <what-built>
    Plans 01-03 (and Task 1 above) have delivered:
    - Tool exposure: apply_event now accepts the 6 encounter events without UUID rejection.
    - Prompt block: the master receives the Combat-lifecycle block (on the vaultMutations-enabled One Piece campaign).
    - Bestiary: 180 SRD monster files at data/vault/handbook/monsters/.
    - Turn interleaving: the route drives handoff from EncounterState.turnOrder in active encounters.
    - sourceOfTruth:'vault': the One Piece campaign now reads state (HP, conditions, combat) from vault files.
    All automated tests pass. What remains is behavioral verification by the operator: does the master actually follow the lifecycle?
  </what-built>
  <how-to-verify>
    Step 1 — Confirm vault state is rendering. Open the One Piece campaign in the UI. The PC mechanics pane (HP, conditions) should render from vault (it was already doing this for HP/conditions since Phase 02). If you see any "vault offline" or missing-state banner, stop and report.

    Step 2 — Start a fight. Send a message that invites combat, for example:
      "Luffy incontra un gruppo di pirati Buggy che bloccano la strada. Inizia lo scontro!"

    Step 3 — Watch the master's turn. The master should:
      (a) Emit combat_start (you may see tool-use in dev console or just the state change).
      (b) Emit monster_spawn for each enemy — if using a standard creature (e.g. Pirata → pirate is not SRD, so the master should invent stats inline in the fat payload; or it may use a bandit / thug from the bestiary).
      (c) Emit initiative_set with the full turn order (Luffy UUID + monster ids).
      (d) The CombatTracker should appear in the UI showing the encounter, round 1, the turn order.

    Step 4 — Verify tracker. Confirm the CombatTracker panel shows:
      - Round number (1)
      - Luffy in the initiative list with his PC entry
      - Monster(s) with their spawned HP

    Step 5 — Play through a full round.
      (e) If it's Luffy's turn first (per initiative): the master should stop and let you act. Type an attack action, e.g.: "Luffy usa il Gomu Gomu no Pistol sull'avversario più vicino!"
      (f) The master narrates the outcome, applies monster_hp_change to reduce the monster's HP, then emits turn_advance. The tracker should update with the new HP.
      (g) On the monster's turn: the master narrates the monster's action, applies hp_change to Luffy (you see Luffy's HP change in the mechanics pane), emits turn_advance.
      (h) Round alternation: continue until one side is defeated.

    Step 6 — End combat.
      (i) When the monster is defeated (hp ≤ 0): the master emits combat_end. The CombatTracker clears (no active encounter).

    Step 7 — Confirm non-combat resumed.
      After combat_end: send a normal non-combat message. The session resumes normal turn handoff (detectAddressee fallback). Verify no errors.

    If any step fails: describe which step and what you observed (e.g. "master emitted initiative_set but CombatTracker did not appear" → likely a snapshot wiring issue; report the specific failure).
  </how-to-verify>
  <resume-signal>
    Type "approved" after confirming steps (a)-(i) pass — tracker appears, turns alternate, HP changes land, combat_end clears it. OR describe the first failure step and what was observed.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator script → campaigns.settings | The _set-source-of-truth.ts script writes a settings JSONB update server-side via Drizzle. No LLM or user input is involved. The value is validated to be 'vault' or 'postgres' before writing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04-01 | Tampering | _set-source-of-truth.ts value validation | mitigate | Script validates value is in {'vault','postgres'} before writing; exits 2 otherwise. No injection vector. |
| T-07-04-02 | Information Disclosure | sourceOfTruth:'vault' — combat state rendered from filesystem | accept | combat.md and characters/<slug>.md contain game-state data (HP, conditions, monsters). This is the intended behavior for vault campaigns. No PII beyond what is already in the Postgres path. Auth gate is the existing session ownership check (unchanged). |
| T-07-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages. |
</threat_model>

<verification>
- pnpm tsx scripts/_set-source-of-truth.ts &lt;one-piece-prefix&gt; vault logs "✓ One Piece ... sourceOfTruth: postgres → vault" (or prior value → vault).
- pnpm tsc --noEmit exits 0 (script is type-clean).
- Operator smoke: CombatTracker renders with round/initiative/HP; turns alternate; combat_end clears tracker; non-combat resumes.
</verification>

<success_criteria>
- [ ] scripts/_set-source-of-truth.ts committed and type-clean.
- [ ] One Piece campaign settings.sourceOfTruth === 'vault'.
- [ ] Operator smoke: master emits combat_start → monster_spawn → initiative_set; CombatTracker appears in UI.
- [ ] Operator smoke: turns alternate — PC acts on PC turn; master runs monster turn + applies monster_hp_change.
- [ ] Operator smoke: combat_end clears the tracker.
- [ ] Operator smoke: non-combat message after combat resumes normal handoff without errors.
</success_criteria>

<output>
Create `.planning/phases/07-vault-combat-playable-d2/07-04-SUMMARY.md` when done.
</output>
