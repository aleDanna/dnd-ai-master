---
phase: 06-vault-combat-state-foundation-d1
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - src/ai/master/vault/snapshot-reader.ts
  - src/sessions/client-snapshot.ts
  - tests/ai/master/vault/combat-snapshot.test.ts
autonomous: true
requirements: [REQ-037]

must_haves:
  truths:
    - "snapshot-reader.ts replaces hard-coded inCombat:false/combat:null with encounter-derived values from the EncounterState in combat.md"
    - "client-snapshot.ts sources actors from the vault encounter view (monster list) for vault campaigns instead of always querying Postgres combat_actors"
    - "A vault campaign mid-encounter surfaces state.inCombat:true, state.combat:{round,currentIdx,turnOrder}, and actors in the CombatActorRow shape with initiative from turnOrder"
    - "After combat_end the snapshot surfaces inCombat:false, combat:null, actors:[]"
    - "A vault campaign with no combat_start in events.md surfaces inCombat:false, combat:null, actors:[] (today's behavior preserved)"
    - "The snapshot mapping exactly matches the CombatActorRow interface (id, sessionId, name, monsterSlug, hpCurrent, hpMax, initiative, isAlive, conditions)"
    - "Postgres combat_actors query is still executed for non-vault (sourceOfTruth==='postgres') sessions"
  artifacts:
    - path: "src/ai/master/vault/snapshot-reader.ts"
      provides: "translateCharacterState returns inCombat and combat derived from EncounterState passed in; materializeFromVault reads combat.md and passes encounter to translator"
      contains: "inCombat"
    - path: "src/sessions/client-snapshot.ts"
      provides: "actors sourced from vault encounter monsters when sourceOfTruth==='vault' and state was vault-materialized; Postgres path unchanged"
      contains: "sourceOfTruth"
    - path: "tests/ai/master/vault/combat-snapshot.test.ts"
      provides: "snapshot-shape tests for mid-encounter and post-combat_end vault campaigns; regression for no-combat and Postgres path"
      contains: "CombatActorRow"
  key_links:
    - from: "materializeFromVault"
      to: "translateCharacterState"
      via: "EncounterState derived from replayEvents(envelopes).encounter passed as argument"
      pattern: "encounter.*translateCharacterState|translateCharacterState.*encounter"
    - from: "client-snapshot.ts vault branch"
      to: "actors array"
      via: "vault state + encounter monsters mapped to CombatActorRow shape"
      pattern: "sourceOfTruth.*vault.*actors|vaultActors"
    - from: "CombatActorRow.initiative"
      to: "EncounterState.turnOrder"
      via: "turnOrder.find(e => e.actorId === monster.id)?.initiative ?? 0"
      pattern: "turnOrder.*initiative|initiative.*turnOrder"
---

<objective>
Wire the EncounterState produced in Plan 01 into the snapshot pipeline: replace the hard-coded
`inCombat:false, combat:null` in snapshot-reader.ts with encounter-derived values, and update
client-snapshot.ts so that vault campaigns source `actors` from the encounter monster list
instead of always querying Postgres `combat_actors`. Verified by headless snapshot-shape tests.

Purpose: Completes REQ-037 D1 — the existing backend-agnostic CombatTracker will render vault
combat state once D2 flips `sourceOfTruth:'vault'` on a live campaign.

Output: Modified snapshot-reader.ts + client-snapshot.ts; new
tests/ai/master/vault/combat-snapshot.test.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-vault-combat-state-foundation-d1/06-CONTEXT.md
@.planning/phases/06-vault-combat-state-foundation-d1/06-01-SUMMARY.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: snapshot-reader.ts — derive inCombat + combat from EncounterState</name>
  <files>src/ai/master/vault/snapshot-reader.ts</files>
  <read_first>
    - src/ai/master/vault/snapshot-reader.ts lines 145-230 — translateCharacterState function: note the `inCombat: false` and `combat: null` hard-codes at lines ~214-215 (the "UI-only / scene-state fields" block). These are the two lines to replace with encounter-derived values. Read the full function signature to determine where to add the new `encounter: EncounterState` parameter.
    - src/ai/master/vault/snapshot-reader.ts line ~92 — materializeFromVault function entry: this is where replayEvents is called (or parseEventsFile is called). Confirm whether materializeFromVault already calls replayEvents or parseEventsFile to build character state. The encounter field from the updated replayEvents return value is extracted here and passed down to translateCharacterState.
    - src/ai/master/vault/projector.ts — EncounterState type (added in Plan 01); replayEvents updated signature returning { chars, encounter }; the import path is already used in snapshot-reader.ts.
    - src/db/schema/session-state.ts — the `combat` column type: `{ round: number; turnOrder: { actorId: string; initiative: number }[]; currentIdx: number } | null`. The snapshot mapping must produce exactly this shape when active:true.
    - .planning/phases/06-vault-combat-state-foundation-d1/06-CONTEXT.md — §"EncounterState + view (LOCKED)" snapshot mapping: `state.inCombat = active`; `state.combat = active ? { round, currentIdx, turnOrder } : null`.
  </read_first>
  <action>
    In src/ai/master/vault/snapshot-reader.ts, make two coordinated changes:

    1. Update translateCharacterState signature to accept a second parameter:
       `encounter: EncounterState` (import EncounterState from projector.ts if not already imported).
       Replace the two hard-coded lines:
         `inCombat: false,`
         `combat: null,`
       with encounter-derived values:
         `inCombat: encounter.active,`
         `combat: encounter.active ? { round: encounter.round, currentIdx: encounter.currentIdx, turnOrder: encounter.turnOrder } : null,`
       The `combat` value shape must exactly match the `session_state.combat` column type from session-state.ts:
       `{ round: number; turnOrder: { actorId: string; initiative: number }[]; currentIdx: number } | null`.
       The `turnOrder` entries in EncounterState already have that exact shape (actorId + initiative).

    2. Update materializeFromVault (line ~92) to extract encounter from the replayEvents result:
       The existing code calls replayEvents(envelopes) and gets back a Map<string,CharacterState>.
       After Plan 01, replayEvents returns `{ chars, encounter }`.
       Update the destructure: `const { chars, encounter } = replayEvents(envelopes)` (or equivalent).
       Pass `encounter` as the second argument to translateCharacterState.
       If materializeFromVault calls replayEvents more than once, update all call sites.

    The function must also return a value for `actors` that is encounter-derived — but that wiring
    lives in client-snapshot.ts (Task 2). snapshot-reader.ts only exposes the encounter-derived
    `inCombat` and `combat` fields on the SessionState-shaped partial.

    Run pnpm tsc --noEmit. Fix any type errors from the signature change (all callers of
    translateCharacterState are internal to snapshot-reader.ts).
  </action>
  <verify>
    <automated>pnpm tsc --noEmit && pnpm vitest run tests/ai/master/vault/snapshot-reader.test.ts</automated>
  </verify>
  <done>
    translateCharacterState accepts EncounterState; inCombat and combat are encounter-derived; existing snapshot-reader.test.ts passes; pnpm tsc --noEmit exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: client-snapshot.ts — vault actors from encounter monsters + snapshot-shape tests</name>
  <files>src/sessions/client-snapshot.ts, tests/ai/master/vault/combat-snapshot.test.ts</files>
  <read_first>
    - src/sessions/client-snapshot.ts lines 75-135 — full vault-branch and Postgres-fallback block. Note: (a) line ~83 `sourceOfTruth === 'vault'` condition; (b) line ~85 `materializeFromVault(campaign.id, viewerCharId, sessionId)` call; (c) line ~117 `const actors = await db.select().from(combatActors)...` — this is the Postgres actors query that always runs. For vault campaigns, after vaultState is populated, actors must come from the encounter instead.
    - src/sessions/client-types.ts lines 27-64 — CombatActorRow interface. Required fields: id (string), sessionId (string), name (string), monsterSlug (string|null), hpCurrent (number), hpMax (number), initiative (number), isAlive (boolean), conditions (array with slug/source/durationRounds/appliedRound). Optional fields: turnState?, position?, senses?. The mapping must satisfy all required fields.
    - src/sessions/client-snapshot.ts lines 130-145 — the return object. Note the `actors` field — this is what needs to conditionally come from vault for vault campaigns.
    - src/ai/master/vault/projector.ts — EncounterState type (monsters array shape: id, name, hpCurrent, hpMax, ac?, initiativeBonus?, isAlive, conditions:string[]). Note: monster.conditions is string[] in EncounterState; CombatActorRow.conditions is an array of objects. The mapping must transform the string slug to the object shape `{ slug, source:'vault-encounter', durationRounds:'until_removed', appliedRound:0 }`.
    - .planning/phases/06-vault-combat-state-foundation-d1/06-CONTEXT.md — §"EncounterState + view (LOCKED)": `actors` = monsters mapped to CombatActorRow shape; per-actor `initiative` from the matching turnOrder entry; PCs are NOT included in actors (tracker looks them up from the party snapshot).
  </read_first>
  <action>
    Two changes: client-snapshot.ts wiring, then the test file.

    CLIENT-SNAPSHOT.TS CHANGE:

    After the vault branch populates `state` from vaultState (line ~91), extract the encounter
    from the vault materialization result. The cleanest approach:

    Option A (preferred if materializeFromVault can be extended): Update materializeFromVault
    to return `{ state: Partial<SessionState>; encounter: EncounterState } | null` and destructure
    both here. The encounter is already built inside materializeFromVault (Task 1 of this plan).

    Option B (if that changes too much): Call replayEvents(envelopes) directly in this module
    for the actor mapping — but this would require re-importing parseEventsFile and eventsPath,
    creating a second replay. Avoid this.

    Use Option A. In snapshot-reader.ts (as an extension to Task 1 if needed), expose the
    encounter from materializeFromVault by updating its return type to include the encounter state.
    In client-snapshot.ts, destructure: `const { state: vaultStatePartial, encounter: vaultEncounter } = await materializeFromVault(...)` (or handle null).

    Then, at line ~117 where actors are populated, add a conditional:
      If `sourceOfTruth === 'vault' && vaultEncounter && vaultEncounter.active`:
        Build vaultActors by mapping `vaultEncounter.monsters` to CombatActorRow:
          For each monster:
            id: monster.id  (the deterministic spawner-assigned string id from monster_spawn payload)
            sessionId: sessionId  (the current session id — required by CombatActorRow)
            name: monster.name
            monsterSlug: null  (D1 has no bestiary; slug is D2)
            hpCurrent: monster.hpCurrent
            hpMax: monster.hpMax
            initiative: vaultEncounter.turnOrder.find(e => e.actorId === monster.id)?.initiative ?? 0
            isAlive: monster.isAlive
            conditions: monster.conditions.map(slug => ({
              slug,
              source: 'vault-encounter',
              durationRounds: 'until_removed' as const,
              appliedRound: 0,
            }))
            (Do NOT include turnState, position, or senses — they are optional and absent in D1)
        Use vaultActors as the actors value; skip the Postgres combat_actors query for this branch.
      Else (Postgres path or no active encounter):
        Run the existing `await db.select().from(combatActors).where(...)` query.
    
    The Postgres query must still run for non-vault sessions. Restructure the actors assignment
    to be conditional rather than always-executing.

    Important: when `vaultEncounter.active === false` (no encounter), vaultActors = [] and the
    Postgres combat_actors query is skipped (vault campaigns never write to Postgres combat_actors,
    so the Postgres query would return [] anyway — skipping it avoids a needless DB round-trip).

    TEST FILE: tests/ai/master/vault/combat-snapshot.test.ts (new).

    This test file validates the snapshot shape end-to-end in a headless fashion. It must NOT
    import any Drizzle DB types, must NOT require DATABASE_URL, and must NOT call any Postgres
    query. Instead, it tests the MAPPING LOGIC by importing the relevant pure functions directly.

    The test approach: import and test the monster→CombatActorRow mapping logic as a unit.
    Extract the monster-to-CombatActorRow mapping to a module-level helper function
    `mapMonsterToActorRow(monster, turnOrder, sessionId): CombatActorRow` in client-snapshot.ts
    (or snapshot-reader.ts if more natural) and export it for testing. If the mapping is inline,
    inline it but also add a `export function buildVaultActors(...)` wrapper that can be imported.

    Test suite A — "CombatActorRow mapping: mid-encounter":
      Input: encounter = {
        active:true, round:2, currentIdx:0,
        turnOrder:[{actorId:'pc-uuid-1',initiative:18},{actorId:'goblin-1',initiative:14}],
        monsters:[{ id:'goblin-1', name:'Goblin', hpCurrent:4, hpMax:7, ac:13, isAlive:true, conditions:[] }]
      }; sessionId='session-uuid-1'.
      Call buildVaultActors(encounter, sessionId).
      Assertions:
        - result.length === 1 (PCs not in actors).
        - result[0].id === 'goblin-1'.
        - result[0].sessionId === 'session-uuid-1'.
        - result[0].name === 'Goblin'.
        - result[0].monsterSlug === null.
        - result[0].hpCurrent === 4.
        - result[0].hpMax === 7.
        - result[0].initiative === 14 (sourced from turnOrder entry matching actorId 'goblin-1').
        - result[0].isAlive === true.
        - result[0].conditions.length === 0.

    Test suite B — "CombatActorRow mapping: condition present":
      Input: monster with conditions:['blinded','poisoned']; matching turnOrder entry present.
      Assert result[0].conditions.length === 2.
      Assert result[0].conditions[0].slug === 'blinded'.
      Assert result[0].conditions[0].source === 'vault-encounter'.
      Assert result[0].conditions[0].durationRounds === 'until_removed'.

    Test suite C — "CombatActorRow mapping: monster not in turnOrder (initiative defaults to 0)":
      Input: encounter with a monster id not present in turnOrder.
      Assert result[0].initiative === 0 (fallback, no throw).

    Test suite D — "buildVaultActors: no active encounter → empty array":
      Input: encounter = { active:false, round:0, currentIdx:0, turnOrder:[], monsters:[] }.
      Assert buildVaultActors(encounter, sessionId).length === 0.

    Test suite E — "Snapshot combat fields: inCombat + combat shape":
      Import translateCharacterState (or a wrapper around the encounter-derived fields).
      Build a minimal CharacterState and EncounterState mid-encounter.
      Assert the returned partial has `inCombat: true` and `combat.round`, `combat.currentIdx`,
      `combat.turnOrder` matching the encounter.
      Build the same with encounter.active:false → assert `inCombat:false, combat:null`.
  </action>
  <verify>
    <automated>pnpm vitest run tests/ai/master/vault/combat-snapshot.test.ts tests/ai/master/vault/snapshot-reader.test.ts && pnpm tsc --noEmit</automated>
  </verify>
  <done>
    All 5 suites in combat-snapshot.test.ts pass. Existing snapshot-reader.test.ts stays green. pnpm tsc --noEmit exits 0. grep confirms 'vault-encounter' as the source string in client-snapshot.ts (or the exported helper). The Postgres combat_actors query is conditional on non-vault path.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| vault EncounterState → snapshot | EncounterState is produced by a pure replay of events.md (a trusted local file owned by the operator). No user-supplied strings reach the snapshot mapping without having first been persisted as a validated event (via the existing validateEvent guard in the dispatcher — D2 surface). |
| snapshot → CombatTracker (client) | CombatActorRow fields are numbers and strings from the validated event payloads. No injection vector exists in the mapping: fields are typed, not eval'd or interpolated into HTML directly. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Information Disclosure | combat.md / EncounterState exposed via snapshot | accept | Snapshot is served only to the authenticated session owner (existing auth gate in client-snapshot.ts is unchanged). Monster HP/name data is game-world information; no PII. |
| T-06-04 | Tampering | monster id as CombatActorRow.id | accept | Monster id is a caller-assigned string from the monster_spawn payload (e.g. 'goblin-1'). It is not a DB primary key and not used for DB lookups. The CombatTracker uses it only for React keying. No privilege escalation vector. |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan. |

No high or medium ASVS L1 threats identified. This plan adds a read-side mapping layer within an already-authenticated server function. No new I/O, no new auth paths, no user-free-text fields reach the snapshot output without passing through the existing event validation boundary.
</threat_model>

<verification>
- `pnpm vitest run tests/ai/master/vault/combat-snapshot.test.ts` exits 0 (all 5 suites pass).
- `pnpm vitest run tests/ai/master/vault/snapshot-reader.test.ts` exits 0 (regression green).
- `pnpm vitest run tests/ai/master/vault/` exits 0 (full vault test suite — regression across all vault tests).
- `pnpm tsc --noEmit` exits 0.
- `grep -c 'vault-encounter' src/sessions/client-snapshot.ts` (or the exported helper file) returns >= 1.
- `grep -c 'inCombat.*encounter\|encounter.*inCombat' src/ai/master/vault/snapshot-reader.ts` returns >= 1.
- `grep -n 'combatActors' src/sessions/client-snapshot.ts` shows the Postgres query is inside a conditional block (not unconditional on line ~117).
</verification>

<success_criteria>
- [ ] translateCharacterState derives inCombat and combat from EncounterState (not hard-coded false/null).
- [ ] materializeFromVault (or its return type) exposes EncounterState so client-snapshot.ts can use it.
- [ ] buildVaultActors (or equivalent inline logic) maps EncounterState.monsters to CombatActorRow[] matching the exact interface in client-types.ts.
- [ ] initiative per actor sourced from turnOrder entry; defaults to 0 if actor not in turnOrder.
- [ ] conditions mapped from string[] to {slug,source:'vault-encounter',durationRounds:'until_removed',appliedRound:0}[].
- [ ] Postgres combat_actors query skipped for vault sessions (encounter provides actors).
- [ ] active:false encounter → inCombat:false, combat:null, actors:[].
- [ ] no-combat_start campaign → same defaults as today (behavior preserved).
- [ ] All 5 suites in combat-snapshot.test.ts pass; snapshot-reader.test.ts and full vault suite stay green.
- [ ] pnpm tsc --noEmit exits 0.
</success_criteria>

<output>
Create `.planning/phases/06-vault-combat-state-foundation-d1/06-02-SUMMARY.md` when done.
</output>
