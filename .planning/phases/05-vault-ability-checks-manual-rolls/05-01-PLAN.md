---
phase: 05-vault-ability-checks-manual-rolls
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/ai/master/vault/prompt-builder.ts
  - src/app/api/sessions/[id]/turn/route.ts
  - tests/ai/master/vault/prompt-builder.test.ts
autonomous: false
requirements: [REQ-036, REQ-022]

must_haves:
  truths:
    - "buildVaultSystemPrompt emits `## Rolls` block when and only when manualRolls === true (per D-gating)"
    - "Block contains 'Easy 10, Medium 15, Hard 20', 'AUTHORITATIVE', bare-d20+modifier rule"
    - "Italian language yields Italian phrasings + anti-mixing clause; English (default) yields English phrasings"
    - "showDifficultyNumbers:false omits numeric DC/AC from examples and adds hidden-difficulty line"
    - "REQ-022 preserved: read-only default (manualRolls undefined) hash = 60e56767b9c63ae936741fc6812a3958c6be346662736a455bed75510c54b14e UNCHANGED"
    - "1000-build byte-stability holds for every new input combination"
    - "turn/route.ts vault branch passes manualRolls + showDifficultyNumbers from userPrefs to buildVaultSystemPrompt"
    - "One Piece campaign has manualRolls=true enabled (operator smoke step)"
  artifacts:
    - path: "src/ai/master/vault/prompt-builder.ts"
      provides: "manualRolls-gated ## Rolls block; VaultPromptInput extended with manualRolls? and showDifficultyNumbers?"
      contains: "## Rolls"
    - path: "tests/ai/master/vault/prompt-builder.test.ts"
      provides: "REQ-022 stability assertions (existing hash unchanged) + gated content assertions for it/en + hideDC + block-absent tests"
      contains: "## Rolls"
    - path: "src/app/api/sessions/[id]/turn/route.ts"
      provides: "manualRolls + showDifficultyNumbers wired from userPrefs at buildVaultSystemPrompt call site"
      contains: "manualRolls"
  key_links:
    - from: "VaultPromptInput.manualRolls"
      to: "## Rolls block emission"
      via: "if (input.manualRolls === true) lines.push(...)"
      pattern: "manualRolls.*true"
    - from: "turn/route.ts buildVaultSystemPrompt call"
      to: "userPrefs.manualRolls / userPrefs.showDifficultyNumbers"
      via: "object spread at lines 296-309"
      pattern: "manualRolls.*userPrefs"
    - from: "## Rolls block phrasings"
      to: "roll-parser.ts parseRollRequests patterns #1a/#1b/#2/#3/#4/#5"
      via: "Italian: 'Tira una prova di'; English: 'Roll a DC'"
      pattern: "Tira una prova di|Roll a DC"
---

<objective>
TDD addition of the manualRolls-gated `## Rolls` block to `buildVaultSystemPrompt`, plus
wiring of `manualRolls` + `showDifficultyNumbers` into the turn-route vault branch,
implementing REQ-036 while preserving REQ-022 byte-stability.

Purpose: Enable the vault-path DM to call for ability checks / saving throws / attack and
damage rolls using the existing 🎲 button surface. The block instructs the master to write
parser-compatible roll requests in prose (the client already renders buttons and handles
results); no changes to the parser, roll components, or engine are needed.

Output: Modified `prompt-builder.ts` (new inputs + gated block), extended
`prompt-builder.test.ts` (content + hash + stability assertions), one-line wiring in
`turn/route.ts`. Followed by an operator smoke step: enable `manualRolls=true` on the One
Piece campaign and verify the 🎲 button appears.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-vault-ability-checks-manual-rolls/05-CONTEXT.md
@src/ai/master/vault/prompt-builder.ts
@tests/ai/master/vault/prompt-builder.test.ts
@src/ai/master/vault/__forbidden-patterns.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED content tests then GREEN — extend VaultPromptInput and emit the gated ## Rolls block</name>
  <files>src/ai/master/vault/prompt-builder.ts, tests/ai/master/vault/prompt-builder.test.ts</files>
  <read_first>
    - src/ai/master/vault/prompt-builder.ts — full file. Note: (a) VaultPromptInput interface for new fields; (b) the applyEventMention gating pattern (line ~102); (c) the roster block gating pattern (lines ~167-180); (d) the insertion point AFTER the roster block and BEFORE the `Respond in language:` clause (line ~181). The block goes at ~line 181 as a new conditional block.
    - tests/ai/master/vault/prompt-builder.test.ts — full file. Note: BASE_INPUT constant, existing "Phase 02 gate" and "Phase 02.1 roster" describe blocks to NOT modify; the "matches the locked snapshot" test and its hardcoded expected hash 60e56767b9c63ae936741fc6812a3958c6be346662736a455bed75510c54b14e (for {vaultRoot:'data/vault', campaignId:'test-camp', toolCount:3}); REQ-022 "1000 builds" tests.
    - .planning/phases/05-vault-ability-checks-manual-rolls/05-CONTEXT.md — §"Exact block content (LOCKED)" for the verbatim three-variant block content; §"Inputs &amp; wiring (LOCKED)" for the two new interface fields; §"Block placement (LOCKED)" for the insertion position.
    - src/ai/master/vault/__forbidden-patterns.ts — REQ-022 patterns the block must NOT introduce.
    - src/ai/master/system-prompt.ts lines ~1710-1789 — buildManualRollsRule reference for proven phrasings; read to confirm exact Italian/English phrasings and anti-mixing clause wording. Do NOT import from it.
  </read_first>
  <behavior>
    New describe block "buildVaultSystemPrompt — Phase 05 rolls block (REQ-036)":

    Test (a): read-only default (BASE_INPUT, manualRolls undefined) → block ABSENT.
      Assertion: `!prompt.includes('## Rolls')`.

    Test (b): manualRolls:false → block ABSENT.
      Input: {...BASE_INPUT, manualRolls:false}. Assertion: `!prompt.includes('## Rolls')`.

    Test (c): manualRolls:true, toolCount:3, vaultMutations undefined (gating independent of vaultMutations) → block PRESENT with required tokens.
      Assertions (all on the same prompt string):
        - `prompt.includes('## Rolls')`
        - `prompt.includes('Easy 10, Medium 15, Hard 20')`
        - `prompt.includes('AUTHORITATIVE')`
        - bare-d20 rule: `prompt.includes('bare d20')` or equivalent (the block says "the button rolls a bare d20")
        - "add … modifier" instruction present for ability checks/saves
        - Default English phrasing: `prompt.includes('Roll a DC 15 Perception check.')`
        - Default English phrasing: `prompt.includes('Roll a DC 14 Dexterity save.')`
        - Default English attack example: `prompt.includes('Roll 1d20+')`
        - Block present regardless of vaultMutations: re-run with {..., vaultMutations:true} → same assertions hold.

    Test (d): language:'it' → Italian phrasings + anti-mixing clause.
      Input: {...BASE_INPUT, manualRolls:true, language:'it'}
      Assertions:
        - `prompt.includes('Tira una prova di Percezione (CD 15).')`
        - `prompt.includes('Tira un TS Destrezza (CD 14).')`
        - Anti-mixing clause present: `prompt.includes('never mix languages')` or equivalent token from the locked block (the exact Italian block says "never mix languages like "Roll una prova di Perception"").

    Test (e): showDifficultyNumbers:false → no numeric DC and hidden-difficulty line present.
      Input: {...BASE_INPUT, manualRolls:true, showDifficultyNumbers:false}
      Assertions:
        - `!prompt.includes('DC 15')` and `!prompt.includes('CD 15')`
        - `prompt.includes('Hidden difficulty')` (the hidden-difficulty appended line)

    Test (f): showDifficultyNumbers:true (explicit) → DC numbers present.
      Input: {...BASE_INPUT, manualRolls:true, showDifficultyNumbers:true}
      Assertions: `prompt.includes('DC 15')` or `prompt.includes('CD 15')`.

    Test (g): REQ-022 byte-stability — 1000 builds of {manualRolls:true, toolCount:3} → 1 hash.
      Same pattern as the existing REQ-022 "1000 builds" tests (use createHash('sha256') or equivalent). All 1000 hashes equal hashes[0].

    Test (h): REQ-022 byte-stability — 1000 builds of {manualRolls:true, language:'it', toolCount:3} → 1 hash.

    Test (i): REQ-022 byte-stability — 1000 builds of {manualRolls:true, showDifficultyNumbers:false, toolCount:3} → 1 hash.

    NOTE: The existing locked-snapshot test that asserts hash 60e56767b9c63ae936741fc6812a3958c6be346662736a455bed75510c54b14e for BASE_INPUT must remain UNCHANGED and PASSING — this is the safety gate that manualRolls is truly off-by-default and additive.
  </behavior>
  <action>
    TDD RED then GREEN. Two sub-steps, single file pair.

    RED sub-step:
    Add the new describe block "buildVaultSystemPrompt — Phase 05 rolls block (REQ-036)" to tests/ai/master/vault/prompt-builder.test.ts with all assertions listed in the behavior block above. Run the file: tests (a) and (b) (absent assertions) and the existing locked-snapshot test PASS because the builder does nothing yet; tests (c) through (i) FAIL (block not emitted). This RED state confirms the tests drive the implementation.

    GREEN sub-step:
    In src/ai/master/vault/prompt-builder.ts:

    1. Extend VaultPromptInput (the interface exported from this file) with two optional fields:
       - `manualRolls?: boolean`
       - `showDifficultyNumbers?: boolean`

    2. Add a module-level helper (or inline the block in the lines array) that builds the ## Rolls section.
       The helper accepts `{ language, hideDC }` (mirroring buildManualRollsRule signature).
       It MUST NOT call Date.now, Math.random, process.env, randomUUID, process.hrtime, or new Date.
       Implement language branching: language === 'it' selects Italian phrasings + Italian anti-mixing clause;
       anything else (including undefined) selects English phrasings + English anti-mixing clause.
       Implement DC visibility: when hideDC is true, strip the "(CD N)" / "(DC N)" / "(CA N)"/"(AC N)"
       tokens from each example line and append a hidden-difficulty paragraph (per 05-CONTEXT.md §"Hidden-difficulty variant").

       Reproduce all asserted tokens verbatim (from 05-CONTEXT.md §"Exact block content (LOCKED)"):
         - Header line: `## Rolls`
         - `uncertain` and `Easy 10, Medium 15, Hard 20` in the intro paragraph
         - `AUTHORITATIVE` in the authoritative-number paragraph
         - `bare d20` + `add … modifier` in the ability-check / saving-throw bullet
         - Italian phrasings: `Tira una prova di Percezione (CD 15).`, `Tira un TS Destrezza (CD 14).`, `Tira 1d20+`, `Tira 2d6+`
         - English phrasings: `Roll a DC 15 Perception check.`, `Roll a DC 14 Dexterity save.`, `Roll 1d20+`, `Roll 2d6+`
         - Anti-mixing token: present in both language variants
       Each physical line is an explicit separate string pushed to lines (not a multi-line template literal) — same paranoia as the existing blocks per prompt-builder.ts file comment.

    3. Insert the gated block into the `lines` array in `buildVaultSystemPrompt`, at the position
       AFTER the roster block (the closing `lines.push('')` at the end of the character roster if-block)
       and BEFORE the `Respond in language:` clause:

       ```
       if (input.manualRolls === true) {
         // ... push the ## Rolls lines ...
         lines.push('');
       }
       ```

       The block is independent of vaultMutations — gated solely on manualRolls === true.

    Run the full test file: all new tests PASS and the existing locked-snapshot + REQ-022 tests remain PASSING.
    Run pnpm tsc --noEmit to confirm no type errors.
  </action>
  <acceptance_criteria>
    - `pnpm vitest run tests/ai/master/vault/prompt-builder.test.ts -t "Phase 05 rolls"` exits 0 (all nine new tests pass).
    - The existing locked-snapshot test (hash 60e56767b9c63ae936741fc6812a3958c6be346662736a455bed75510c54b14e) passes UNCHANGED. Verify the hash constant in the test file is unmodified: `grep "60e56767b9c63ae936741fc6812a3958c6be346662736a455bed75510c54b14e" tests/ai/master/vault/prompt-builder.test.ts` returns at least 1 hit.
    - `grep -c '## Rolls' src/ai/master/vault/prompt-builder.ts` returns at least 1.
    - No forbidden pattern introduced: `grep -nE 'Date\.now|Math\.random|process\.env|new[[:space:]]+Date|randomUUID|process\.hrtime' src/ai/master/vault/prompt-builder.ts` returns no matches in the newly added block lines.
    - `pnpm tsc --noEmit` exits 0.
    - The following tokens are grep-findable in prompt-builder.ts: `Easy 10, Medium 15, Hard 20`, `AUTHORITATIVE`, `bare d20`, `Tira una prova di`, `Roll a DC`.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run tests/ai/master/vault/prompt-builder.test.ts && pnpm tsc --noEmit</automated>
  </verify>
  <done>
    The `## Rolls` block is emitted when manualRolls===true and absent when false/undefined;
    all Phase 05 content/gating/stability assertions pass; existing locked-snapshot hash unchanged;
    no forbidden patterns; typecheck clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire manualRolls + showDifficultyNumbers into the turn-route vault branch</name>
  <files>src/app/api/sessions/[id]/turn/route.ts</files>
  <read_first>
    - src/app/api/sessions/[id]/turn/route.ts lines 290-315 — the buildVaultSystemPrompt({...}) call site (the "vault branch", per 05-CONTEXT.md ~lines 296-309). Read the object literal passed to the function to understand existing keys (vaultRoot, campaignId, toolCount, vaultMutations, characters, language, etc.).
    - src/app/api/sessions/[id]/turn/route.ts line ~625 — the baked branch's buildVaultSystemPrompt call already passing showDifficultyNumbers: userPrefs.showDifficultyNumbers. Confirm the field name.
    - src/lib/preferences.ts lines ~240-260 — confirm userPrefs.manualRolls (default false, line ~245) and userPrefs.showDifficultyNumbers (default true, line ~256) are resolved before the call site.
  </read_first>
  <action>
    In src/app/api/sessions/[id]/turn/route.ts, locate the vault-branch buildVaultSystemPrompt({...}) call
    at approximately lines 296-309 (per 05-CONTEXT.md §"Inputs &amp; wiring (LOCKED)"). Add two fields to
    the existing object literal being passed:

      manualRolls: userPrefs.manualRolls,
      showDifficultyNumbers: userPrefs.showDifficultyNumbers,

    Both fields are already resolved on userPrefs (confirmed from preferences.ts). Do NOT change any
    other argument, logic, or file. This is a two-line addition; the baked branch at ~line 625 already
    passes showDifficultyNumbers and serves as the style precedent.

    Run pnpm tsc --noEmit to confirm no type errors from the new fields (VaultPromptInput now declares
    them as optional, so existing call sites without them remain valid).
  </action>
  <acceptance_criteria>
    - `grep -n "manualRolls.*userPrefs\|userPrefs.*manualRolls" src/app/api/sessions/[id]/turn/route.ts` returns at least 1 match inside the vault-branch call block (lines 296-309 region).
    - `grep -n "showDifficultyNumbers.*userPrefs\|userPrefs.*showDifficultyNumbers" src/app/api/sessions/[id]/turn/route.ts` returns at least 2 matches (the baked branch line ~625 + the new vault-branch line).
    - `pnpm tsc --noEmit` exits 0.
    - No other logic changed: `git diff src/app/api/sessions/[id]/turn/route.ts` shows only the two new field lines added inside the vault-branch object literal.
  </acceptance_criteria>
  <verify>
    <automated>pnpm tsc --noEmit</automated>
  </verify>
  <done>
    manualRolls and showDifficultyNumbers from userPrefs are forwarded into the vault-branch
    buildVaultSystemPrompt call; typecheck clean; no other changes.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator smoke — enable manualRolls on One Piece campaign and verify 🎲 button</name>
  <what-built>
    Tasks 1 and 2 above have shipped a fully automated, typecheck-clean, test-green implementation.
    The ## Rolls block is now emitted for any campaign where manualRolls=true. What remains is a
    DATA change (campaign settings) on the One Piece campaign and a functional smoke.
  </what-built>
  <how-to-verify>
    Step 1 — Enable manualRolls on the One Piece campaign. Choose ONE of:

      Option A (Settings UI): Open the One Piece campaign in the app → Settings → toggle "Manual rolls" ON.

      Option B (DB update script): Run the one-off script pattern from scripts/_set-campaign-model.ts
      adapted for manualRolls. Example invocation (adjust for actual script):
        pnpm tsx scripts/_set-campaign-model.ts --campaign &lt;one-piece-id&gt; --field manualRolls --value true

      Option C (direct DB): Use your DB client to set manualRolls=true for the One Piece campaign record.

    Step 2 — Start a new vault-path turn in the One Piece campaign. Write a message that invites an
    uncertain outcome, for example: "esamino con attenzione la stanza alla ricerca di trappole".

    Step 3 — Verify:
      (a) The master's reply contains a roll request in Italian parser-compatible form, e.g.
          "Tira una prova di Percezione (CD 15)." (or similar — the exact DC may vary).
      (b) A 🎲 roll button appears in the narrative pane next to the roll request.
      (c) Tapping the button resolves the roll and the master continues the narrative with the result.

    Step 4 — Verify block-absent for a campaign with manualRolls=false (any other campaign):
      Start a turn in any non-One-Piece campaign → master DOES NOT write Italian/English roll requests
      (no regression introduced).
  </how-to-verify>
  <resume-signal>
    Type "approved" after confirming steps (a), (b), (c) pass, OR describe any issue observed.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → buildVaultSystemPrompt | manualRolls and showDifficultyNumbers are booleans resolved from userPrefs (server-side); they originate from user settings already validated at the preferences layer. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | ## Rolls block (showDifficultyNumbers:true) | accept | Numeric DCs are game-world information shown to the player intentionally; no PII or system-internal data. showDifficultyNumbers:false path explicitly omits them on user request. |
| T-05-02 | Tampering | manualRolls / showDifficultyNumbers inputs | accept | Both are booleans resolved server-side from userPrefs (preferences.ts); no client string is passed directly into the block. The block itself is static/deterministic given those two booleans — no injection vector. |
| T-05-03 | Tampering | language pass-through into block phrasings | accept | The `language` field already flows into the existing "Respond in language:" clause unchanged; this phase adds a parallel branch on the same value. The block uses the value only as a language-selector switch (==='it' vs else), not as a string interpolated into sensitive positions. |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this phase. |

No high or medium ASVS L1 threats identified. This phase adds a static, deterministic string to a system prompt and reads two already-validated booleans from userPrefs. No new input parsing, I/O surface, auth path, or injection vector is introduced.
</threat_model>

<verification>
- `pnpm vitest run tests/ai/master/vault/prompt-builder.test.ts` → all tests pass, including the unchanged locked-snapshot hash 60e56767b9c63ae936741fc6812a3958c6be346662736a455bed75510c54b14e.
- `pnpm tsc --noEmit` → 0 errors.
- `grep "60e56767b9c63ae936741fc6812a3958c6be346662736a455bed75510c54b14e" tests/ai/master/vault/prompt-builder.test.ts` → at least 1 hit (hash unmodified).
- `grep -nE 'Date\.now|Math\.random|process\.env|new[[:space:]]+Date|randomUUID|process\.hrtime' src/ai/master/vault/prompt-builder.ts` → no matches in the new block.
- Operator smoke: 🎲 button appears in One Piece turn when master writes an Italian roll request; absent on non-enabled campaigns.
</verification>

<success_criteria>
- [ ] `## Rolls` block emitted in buildVaultSystemPrompt when manualRolls===true; absent when false/undefined.
- [ ] Block contains verbatim asserted tokens: "Easy 10, Medium 15, Hard 20"; "AUTHORITATIVE"; bare-d20 + modifier rule; four phrasings per language variant; anti-mixing clause.
- [ ] REQ-022: read-only default hash 60e56767b9c63ae936741fc6812a3958c6be346662736a455bed75510c54b14e UNCHANGED.
- [ ] 1000-build stability for {manualRolls:true}, {manualRolls:true, language:'it'}, {manualRolls:true, showDifficultyNumbers:false}.
- [ ] showDifficultyNumbers:false → no DC/CD numbers in examples + hidden-difficulty line present.
- [ ] language:'it' → Italian phrasings; default → English phrasings.
- [ ] turn/route.ts vault branch passes manualRolls + showDifficultyNumbers from userPrefs.
- [ ] No forbidden patterns (REQ-022 lint + lint-test green).
- [ ] pnpm tsc --noEmit exits 0.
- [ ] One Piece campaign manualRolls=true; 🎲 button appears in a smoke turn.
</success_criteria>

<output>
Create `.planning/phases/05-vault-ability-checks-manual-rolls/05-01-SUMMARY.md` when done.
</output>
