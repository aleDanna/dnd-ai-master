---
phase: 08-server-side-combat-resolver-v1-player-attacks
plan: 03
type: execute
wave: 2
depends_on: ["08-01", "08-02"]
files_modified:
  - src/app/api/sessions/[id]/turn/route.ts
autonomous: false
requirements: [REQ-039]

must_haves:
  truths:
    - "On a roll-result during an active vault encounter, the route resolves it server-side BEFORE the LLM loop and emits the resolver's events to events.md"
    - "The narration-only loop runs with the resolver's directive; the LLM's combat-event apply_event calls are dropped that turn (no double-apply)"
    - "On a hit, the damage-request 🎲 button reliably appears (LLM-included or safety-net appended)"
    - "The 07-03 post-loop handoff still runs and re-reads the post-turn_advance encounter (hands to the next PC)"
    - "Non-combat turns and unparseable rolls run EXACTLY as Phase 07 (gate does not fire; regression holds)"
  artifacts:
    - path: "src/app/api/sessions/[id]/turn/route.ts"
      provides: "Vault-branch hook: early encounter read, gate (D-01), resolveCombat, server-side emit (D-06), narration-only loop + directive, safety-net append, D-07 suppression"
      contains: "resolveCombat"
  key_links:
    - from: "src/app/api/sessions/[id]/turn/route.ts"
      to: "src/app/api/sessions/[id]/turn/combat-resolver.ts"
      via: "import resolveCombat; call on the gated roll-result"
      pattern: "resolveCombat\\("
    - from: "src/app/api/sessions/[id]/turn/route.ts"
      to: "src/ai/master/vault/tools.ts"
      via: "dispatchVaultTool('apply_event', ev, {campaignId}) per resolver event"
      pattern: "dispatchVaultTool\\('apply_event'"
    - from: "src/app/api/sessions/[id]/turn/route.ts"
      to: "src/ai/master/vault/loop.ts"
      via: "runVaultToolLoop called with suppressCombatMutations:true on resolution turns"
      pattern: "suppressCombatMutations"
    - from: "src/app/api/sessions/[id]/turn/route.ts"
      to: "src/lib/roll-parser.ts"
      via: "parseRollRequests on finalText to decide the safety-net append"
      pattern: "parseRollRequests"
---

<objective>
Wire the resolver into the vault branch of the turn route — the integration glue (~10 lines of
genuinely new code per RESEARCH, but the SEAMS are the risk). In the vault branch: read the
encounter EARLY (duplicate the existing post-loop read up), gate on `vaultMutations &&
encounter.active && isRollResult(_playerMessage)` (D-01), call `resolveCombat`, emit the
resolver's events server-side via `dispatchVaultTool` (D-06), run `runVaultToolLoop` in
NARRATION-ONLY mode (`suppressCombatMutations:true`) with the resolver's directive, suppress the
player-side resolve directive (D-07 via `serverResolved`), and append the damage-request as a
SAFETY NET if the LLM omitted it. When the gate does NOT fire (not a roll-result, no active
combat, or `resolveCombat` returns `null`), the turn runs EXACTLY as today.

Purpose: This is where REQ-039 becomes observable end-to-end — the server decides hit/miss and
HP, the LLM only narrates. Depends on Plan 01 (`resolveCombat`) and Plan 02 (the loop flag +
D-07), so it is Wave 2.

Output: `route.ts` vault-branch modifications. The 07-03 post-loop handoff read at :454-455 is
PRESERVED (it re-reads after the resolver's `turn_advance` so the handoff sees advanced state).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-server-side-combat-resolver-v1-player-attacks/08-CONTEXT.md
@.planning/phases/08-server-side-combat-resolver-v1-player-attacks/08-RESEARCH.md
@.planning/phases/08-server-side-combat-resolver-v1-player-attacks/08-PATTERNS.md
@.planning/phases/08-server-side-combat-resolver-v1-player-attacks/08-VALIDATION.md
@src/app/api/sessions/[id]/turn/combat-resolver.ts
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Read the Next.js 16 route-handler guide before touching route.ts (per AGENTS.md)</name>
  <action>
    MANDATORY pre-edit read gate (no code change). Per AGENTS.md this is a NON-STANDARD Next.js
    with breaking changes — the executor must read `node_modules/next/dist/docs/01-app/01-getting-started/15-route-handlers.md`
    before editing `route.ts`, and confirm the POST handler signature matches the guide. RESEARCH
    verified `params` is a `Promise` (already handled in route.ts) and the POST signature is
    UNCHANGED — but read the guide to heed any deprecation notice that affects the vault-branch
    edit. This is a human-confirmed gate because it is a project-mandated read, not something the
    automated structure can prove.
  </action>
  <read_first>
    - node_modules/next/dist/docs/01-app/01-getting-started/15-route-handlers.md — the project's NON-STANDARD Next.js guide (per AGENTS.md, breaking changes vs training data). RESEARCH confirmed `params` is a `Promise` (already handled in route.ts) and the POST signature is UNCHANGED — but read it to heed any deprecation notice before editing the handler.
    - AGENTS.md — the project rule: "Read the relevant guide in node_modules/next/dist/docs/ before writing any code. Heed deprecation notices."
  </read_first>
  <what-built>Nothing yet — this is a mandatory pre-edit read gate. The executor reads the Next.js 16 route-handler guide so the route.ts edits in Task 2 honor the non-standard framework (no assumptions from training data).</what-built>
  <how-to-verify>
    1. Confirm `node_modules/next/dist/docs/01-app/01-getting-started/15-route-handlers.md` was read.
    2. Confirm the POST handler signature in `route.ts` matches the guide (RESEARCH: `params` is a `Promise`, already handled; POST signature unchanged) and note any deprecation that affects the vault-branch edit.
  </how-to-verify>
  <resume-signal>Type "read" once the route-handler guide is read and the POST signature is confirmed unchanged (or note any deprecation to account for).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Hook resolveCombat into the vault branch — early read, gate, emit, narration-only loop, safety-net</name>
  <files>src/app/api/sessions/[id]/turn/route.ts</files>
  <read_first>
    - src/app/api/sessions/[id]/turn/route.ts — the file being modified. Use the VERIFIED current anchors (936 lines; CONTEXT line refs drifted — RESEARCH Pitfall 5): vault branch `:278`; `vaultMutationsEnabled` `:282`; clean `_playerMessage` captured at `:355-357` (BEFORE the directive append at `:358-372`); `runVaultToolLoop` call `:379-421`; post-loop transaction opens `:427`; POST-LLM encounter read to DUPLICATE UP `:454-455`; 07-03 `resolveCombatHandoff` `:456`; vault early-return `:523`. Existing imports: `parseEventsFile`/`replayEvents` (`:37`), `eventsPath` (`:38`), `resolveCombatHandoff` (`:36`), `VAULT_TOOL_DEFINITIONS` (`:28`) — but NOT `dispatchVaultTool` and NOT `parseRollRequests` (import deltas below).
    - src/app/api/sessions/[id]/turn/combat-resolver.ts — the `resolveCombat` contract from Plan 01 (return shape, `null` fall-through).
    - src/ai/master/vault/turn-directive.ts (`isRollResult` :69 — reuse for the gate; `buildTurnDirective` :86 — pass `serverResolved` from Plan 02; `appendDirectiveToHistory` — inject the resolver's narrationDirective)
    - src/ai/master/vault/tools.ts (lines 251-383 `dispatchVaultTool` — the single-write emit path; :285 the UUID guard is relaxed for ENCOUNTER_EVENT_TYPES; :371-379 validate→EventsWriter.applyEvent→regenerate combat.md)
    - src/ai/master/vault/loop.ts (the `suppressCombatMutations` flag added in Plan 02 — pass it true on resolution turns)
    - src/lib/roll-parser.ts (`parseRollRequests` — used on `finalText` for the safety-net: append the damage request only if no damage roll-request is already present)
    - 08-PATTERNS.md § "route.ts (MODIFY)" Patterns A/B/C/D — the exact in-file deltas and the two-reads rationale
    - 08-RESEARCH.md § "Architecture Patterns" (the system diagram), "Pattern 3" (two reads), "Code Examples" (the emit loop), Pitfall 4 (gate on the CLEAN message), Open Q3 (safety-net detection via `parseRollRequests(finalText).some(r => r.kind === 'damage')`)
  </read_first>
  <action>
    Modify ONLY the vault branch of `src/app/api/sessions/[id]/turn/route.ts`. The non-combat path
    MUST stay byte-identical to today (regression-protected). Add two import deltas at the top:
    `import { dispatchVaultTool } from '@/ai/master/vault/tools';` (route currently imports only
    `VAULT_TOOL_DEFINITIONS` from that module — extend it) and `import { resolveCombat } from
    './combat-resolver';` and `import { parseRollRequests } from '@/lib/roll-parser';` and
    `import { isRollResult } from '@/ai/master/vault/turn-directive';` (if not already imported).

    1. EARLY ENCOUNTER READ (Pattern A delta — insert between the clean `_playerMessage` capture at
       `:357` and the directive build at `:358`): DUPLICATE the post-loop read up to here —
       `const { encounter } = replayEvents(await parseEventsFile(eventsPath(campaign.id)));`
       (`parseEventsFile`/`replayEvents`/`eventsPath` are already imported). Use the SERVER campaign
       id (`campaign.id`), never anything player-derived.

    2. GATE (D-01): `const serverShouldResolve = vaultMutationsEnabled && encounter.active &&
       isRollResult(_playerMessage);` Gate on the CLEAN `_playerMessage` from `:355-357` (BEFORE the
       directive is appended — RESEARCH Pitfall 4).

    3. RESOLVE: when the gate is true, `const resolver = resolveCombat({ rollResult: _playerMessage!,
       encounter });`. If `resolver === null` → treat as gate-false (fall through to today's path:
       the normal `buildTurnDirective` + `runVaultToolLoop` + 07-03 handoff, unchanged).

    4. EMIT (Pattern C / D-06): when `resolver !== null`, emit each event server-side BEFORE the loop:
       `for (const ev of resolver.events) { const r = await dispatchVaultTool('apply_event', ev,
       { campaignId: campaign.id }); if (r.isError) console.warn('[turn]', sessionId, 'resolver emit
       failed:', r.content); }` — wrap defensively so a dispatcher error logs + continues (D-10,
       never hard-fail the turn). This validates, allocates the UUID, persists to events.md, and
       regenerates combat.md (encounter events skip the UUID guard at tools.ts:285).

    5. DIRECTIVE (D-07 + Pattern D): pass `serverResolved: resolver !== null` into the existing
       `buildTurnDirective(...)` call so the player-side resolve directive is suppressed on the
       resolution turn (Plan 02 flag). Then inject `resolver.narrationDirective` into `vaultHistory`
       via `appendDirectiveToHistory` (recency) INSTEAD of relying on the normal directive for this
       turn. Non-combat turns: `serverResolved` is false → directive behavior unchanged.

    6. NARRATION-ONLY LOOP (Pattern D): on the resolution turn, pass `suppressCombatMutations: true`
       into the `runVaultToolLoop({...})` call (`:379-421`) so the LLM's combat-event `apply_event`
       calls are dropped (Plan 02 flag — no double-apply). Non-combat turns pass it falsy → Phase 07
       behavior unchanged.

    7. SAFETY NET (D-06 / RESEARCH Open Q3): after the loop, if `resolver?.damageRequest` is set AND
       the LLM output lacks a damage roll-request — `!parseRollRequests(vaultResult.finalText).some(r
       => r.kind === 'damage')` — append `resolver.damageRequest` to the final text so the 🎲 damage
       button reliably appears. Use `parseRollRequests` (the same parser the client uses) to detect
       presence; do NOT invent an ad-hoc regex. The exact append mechanism is Claude's discretion.

    8. PRESERVE the 07-03 handoff (Pattern B): DO NOT delete the post-loop encounter read at
       `:454-455` — it re-reads AFTER the resolver's `turn_advance` is persisted, so
       `resolveCombatHandoff` (`:456`) sees the advanced state and hands to the next PC (RESEARCH
       Pattern 3 — the early read and this late read are intentionally two reads of the same file at
       different times). The vault early-return at `:523` is unchanged.

    The exact placement/structure of the early read and the gate is Claude's discretion (CONTEXT)
    as long as: the non-combat path is byte-identical, the gate uses the clean message, the emit
    happens before the loop, and the 07-03 post-loop handoff still works on advanced state.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `route.ts` imports `dispatchVaultTool` from `@/ai/master/vault/tools` and `resolveCombat` from `./combat-resolver` (`grep -E "dispatchVaultTool|resolveCombat" 'src/app/api/sessions/[id]/turn/route.ts'` matches both)
    - `route.ts` calls `dispatchVaultTool('apply_event', ev, { campaignId: campaign.id })` in a loop over `resolver.events` (`grep -F "dispatchVaultTool('apply_event'" 'src/app/api/sessions/[id]/turn/route.ts'` matches)
    - `route.ts` passes `suppressCombatMutations` into `runVaultToolLoop` and `serverResolved` into `buildTurnDirective` (`grep -E 'suppressCombatMutations|serverResolved' 'src/app/api/sessions/[id]/turn/route.ts'` matches both)
    - `route.ts` uses `parseRollRequests` for the safety-net append (`grep -F 'parseRollRequests' 'src/app/api/sessions/[id]/turn/route.ts'` matches)
    - The post-loop encounter read at the 07-03 handoff is still present (`grep -F 'resolveCombatHandoff' 'src/app/api/sessions/[id]/turn/route.ts'` still matches — NOT deleted)
    - The gate reads the clean `_playerMessage` captured before the directive append (`isRollResult(_playerMessage)` appears in the gate)
    - `pnpm tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Vault branch hooks resolveCombat before the loop: early read, gate on clean message, server-side emit, narration-only loop with the resolver directive, D-07 suppression, safety-net append; 07-03 handoff preserved; non-combat path unchanged; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: Affected-surface regression — full vault suite + typecheck green</name>
  <files>src/app/api/sessions/[id]/turn/route.ts</files>
  <read_first>
    - 08-VALIDATION.md § "Sampling Rate" — the per-wave merge command and the phase-gate command (full suite + tsc); the note that the lone pre-existing `applicator/gp-stack` failure (07-03) must remain the ONLY pre-existing failure.
    - 08-RESEARCH.md § "Validation Architecture" → "Sampling Rate" — same gates (baseline ~661 vault tests).
  </read_first>
  <action>
    No code change unless a regression is found. Run the affected-surface suite and the phase gate
    to confirm the route wiring did not regress Phase 07 behavior:
    (1) `pnpm vitest run tests/app/api tests/ai/master/vault tests/sessions` — resolver + loop +
        directive + turn-interleaving (the surface this phase touches). All green.
    (2) `pnpm test` (full suite) — confirm the ONLY pre-existing failure is the `applicator/gp-stack`
        one noted in 07-03; no NEW failures.
    (3) `pnpm tsc --noEmit` — clean.
    If any NEW failure appears (e.g. the non-combat handoff regressed, or a vault test broke), fix
    `route.ts` so the non-combat path is byte-identical to Phase 07 (the gate must not fire on
    non-roll turns; `resolveCombat` null must fall through to the unchanged path) before re-running.
  </action>
  <verify>
    <automated>pnpm vitest run tests/app/api tests/ai/master/vault tests/sessions</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run tests/app/api tests/ai/master/vault tests/sessions` exits 0 (resolver, loop, turn-directive, turn-interleaving all green)
    - `pnpm test` shows no NEW failures vs the 07-03 baseline (the `applicator/gp-stack` failure remains the only pre-existing one)
    - `pnpm tsc --noEmit` exits 0
    - The non-combat turn path is unchanged: the existing turn-interleaving / handoff tests in `tests/sessions` stay green (regression for non-combat multiplayer handoff)
  </acceptance_criteria>
  <done>Affected-surface suite + full suite + typecheck green; no new failures vs the 07-03 baseline; non-combat regression holds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Operator smoke — end-to-end player attack on One Piece (qwen3)</name>
  <action>
    Operator-only verification (no code change unless a defect is found). The two automated layers
    (resolver unit in Plan 01, loop/directive integration in Plan 02) are already green; this
    checkpoint verifies the live model-narration obedience + full client UX end-to-end, which
    cannot run headless (needs a live local LLM on the operator's M4/M5 per REQ-030). The operator
    runs the smoke against the One Piece campaign and confirms the server decides hit/miss, HP
    drops once on a hit, the turn advances, narration matches mechanics, and non-combat turns are
    unchanged.
  </action>
  <what-built>
    The full v1 server-side player-attack resolver, wired end-to-end: on a roll-result during an
    active vault encounter the route resolves hit/miss vs the monster AC server-side, emits
    monster_hp_change + turn_advance, runs the LLM narration-only (its combat-event apply_event
    calls dropped), and appends the 🎲 damage button if the model omitted it. The two automated
    layers (resolver unit + loop/directive integration) are already green; this verifies the live
    model-narration obedience + full client UX, which cannot run headless.
  </what-built>
  <how-to-verify>
    On the One Piece campaign (sourceOfTruth:'vault', qwen3:30b-a3b-instruct-2507-q4_K_M), with
    combat active against Veyra:
    1. Attack Veyra (the client renders the 🎲 to-hit button; tap it).
    2. Confirm the SERVER decides hit/miss vs Veyra's AC — the narration matches the rolled total
       (no more "narrated a MISS on an 18 vs AC 14"); the model no longer free-narrates the outcome.
    3. On a HIT: the 🎲 damage button appears (LLM-included or safety-net appended). Tap it.
    4. Confirm Veyra's HP DROPS in the CombatTracker by the rolled damage (single application — NOT
       doubled; the LLM's own combat-event calls were dropped → no double-apply).
    5. Confirm the turn ADVANCES after the damage resolves (07-03 handoff to the next PC).
    6. Confirm narration matches mechanics with no contradiction.
    7. Sanity: a NON-combat turn (e.g. a rules question) behaves exactly as before (gate did not fire).
    `scripts/_probe-combat.ts` `resolve` mode simulates the exact roll-result string format if a
    headless reproduction is wanted first.
  </how-to-verify>
  <resume-signal>Type "approved" if the server decides hit/miss, HP drops once on a hit, the turn advances, narration matches mechanics, and non-combat turns are unchanged — or describe the contradiction observed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → route (vault branch) | The player-controlled roll-result string arrives as the turn message; the resolver parses it and the route emits events on its behalf. |
| route → EventsWriter | The server emits authoritative `monster_hp_change`/`turn_advance` to events.md via the dispatcher. |
| LLM → dispatcher (resolution turn) | The narrating LLM may ALSO emit combat-event apply_event calls — dropped this turn. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Tampering (integrity) | NO-DOUBLE-APPLY — server + LLM both emitting corrupts HP/turn | mitigate | THE headline integrity control of this phase. The route passes `suppressCombatMutations:true` (Plan 02 loop drop of ENCOUNTER_EVENT_TYPES) AND `serverResolved:true` (Plan 02 D-07 directive suppression) on resolution turns — don't ask, and don't honor if asked. The server's events are the sole authoritative source. Verified at the loop layer (Plan 02) + the operator smoke (Task 4 step 4: HP drops ONCE, not doubled). |
| T-08-06 | Tampering / Elevation | LLM- or player-supplied `campaignId` writes to another campaign's events.md | mitigate | UNCHANGED & preserved: the emit loop passes `campaign.id` (server-side), never anything player-derived; the dispatcher rejects non-UUID (`tools.ts:262`) and errors without a server `campaignId` (`:256`). |
| T-08-07 | DoS | crafted roll string → resolver/route exception → 500 | mitigate | `resolveCombat` never throws → `null` → normal turn (Plan 01). The route's emit loop is wrapped defensively (D-10) so a dispatcher error logs + continues. The route is also already behind Clerk `auth()` + `checkPartyAccess` (`route.ts:94-119`) and the per-turn lock (`acquireTurnLock`) — this phase adds no new entry point. |
| T-08-08 | Tampering | player names an arbitrary monster id to damage | mitigate | The resolver matches by NAME → resolves the server-side `id` (Plan 01); the route emits only resolver-produced events. The reducer defensively skips unknown ids (`projector.ts:784`) and clamps HP at 0 (`:786`). |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No external packages added (RESEARCH § Package Legitimacy Audit — pure TS reusing internal modules; vitest already present). No install task; no slopcheck checkpoint needed. |
</threat_model>

<verification>
- `pnpm tsc --noEmit` clean.
- `pnpm vitest run tests/app/api tests/ai/master/vault tests/sessions` green (affected surface).
- `pnpm test` (phase gate) — only the pre-existing `applicator/gp-stack` failure; no new failures.
- Operator smoke (Task 4): server decides hit/miss; HP drops once on a hit; turn advances; narration matches mechanics; non-combat turns unchanged.
</verification>

<success_criteria>
- A player attack roll during active vault combat is resolved server-side (hit/miss vs the monster AC) before the LLM runs — the model no longer decides the outcome (REQ-039).
- On a hit, damage applies via `monster_hp_change` and the monster's HP drops once in the CombatTracker; the turn advances via `turn_advance` (07-03 handoff to the next PC).
- The LLM narrates the server-determined outcome with no contradiction; its own combat-event apply_event calls are dropped (no double-apply).
- Non-combat turns and unparseable rolls run exactly as Phase 07 (gate does not fire; regression holds).
</success_criteria>

<output>
Create `.planning/phases/08-server-side-combat-resolver-v1-player-attacks/08-03-SUMMARY.md` when done.
</output>
