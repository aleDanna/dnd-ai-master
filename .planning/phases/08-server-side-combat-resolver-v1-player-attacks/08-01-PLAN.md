---
phase: 08-server-side-combat-resolver-v1-player-attacks
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/sessions/[id]/turn/combat-resolver.ts
  - tests/app/api/sessions/[id]/turn/combat-resolver.test.ts
autonomous: true
requirements: [REQ-039]

must_haves:
  truths:
    - "A player to-hit roll whose total >= the monster AC resolves to a HIT and produces a damage-request"
    - "A player to-hit roll whose total < AC resolves to a MISS, emits turn_advance, and produces no damage-request"
    - "A natural 20 below AC still HITS; a natural 1 at/above AC still MISSES"
    - "A player damage roll emits monster_hp_change(id, -total) then turn_advance"
    - "An unknown / ambiguous / non-combat / unparseable roll returns null (resolver never throws)"
    - "The emitted damage-request round-trips through the client parser carrying the target name (per danni a <target> form)"
  artifacts:
    - path: "src/app/api/sessions/[id]/turn/combat-resolver.ts"
      provides: "Pure resolveCombat + parsing helpers (D-02..D-05, D-08)"
      exports: ["resolveCombat", "ResolveCombatResult"]
      min_lines: 80
    - path: "tests/app/api/sessions/[id]/turn/combat-resolver.test.ts"
      provides: "Headless REQ-039 resolver-math unit suite (Wave 0 per 08-VALIDATION.md)"
      contains: "resolveCombat"
  key_links:
    - from: "src/app/api/sessions/[id]/turn/combat-resolver.ts"
      to: "src/ai/master/vault/events-schema.ts"
      via: "import type { VaultEvent } — events returned as plain {type,payload}"
      pattern: "VaultEvent"
    - from: "tests/app/api/sessions/[id]/turn/combat-resolver.test.ts"
      to: "src/lib/roll-parser.ts"
      via: "parseRollRequests asserts the damageRequest round-trips with the target"
      pattern: "parseRollRequests"
---

<objective>
Create the pure `resolveCombat` function (and its parsing helpers) in a new file
`src/app/api/sessions/[id]/turn/combat-resolver.ts`, plus its headless unit-test suite.
This is the dependency-free heart of REQ-039: given the player's roll-result string and the
active `EncounterState`, it deterministically decides to-hit vs damage, hit vs miss against
the monster AC, and returns the events to emit (`monster_hp_change` / `turn_advance`), a
narration directive, and the damage-request string — or `null` to fall through to the normal
LLM turn.

Purpose: Move PLAYER-attack mechanical resolution out of the LLM (which free-narrates outcomes
and ignores the rolled number — the D2-smoke ceiling) into code. This plan delivers ONLY the
pure function + tests; the route wiring and loop suppression are separate plans.

Output: `combat-resolver.ts` (pure, no I/O, no clock, no randomness — headless-testable) and
`combat-resolver.test.ts` covering every REQ-039 resolver-math row from 08-VALIDATION.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-server-side-combat-resolver-v1-player-attacks/08-CONTEXT.md
@.planning/phases/08-server-side-combat-resolver-v1-player-attacks/08-RESEARCH.md
@.planning/phases/08-server-side-combat-resolver-v1-player-attacks/08-PATTERNS.md
@.planning/phases/08-server-side-combat-resolver-v1-player-attacks/08-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the pure resolveCombat function + parsing helpers</name>
  <files>src/app/api/sessions/[id]/turn/combat-resolver.ts</files>
  <read_first>
    - src/app/api/sessions/[id]/turn/combat-handoff.ts — THE structural sibling (same dir, same "Phase NN pure helper called by the vault branch", same JSDoc-with-threat-model header, same discriminated-union `export type`, same `args:{...}` single-object param, same early-return gate style, same `party.some(...)` membership idiom). Copy this shape wholesale and swap the body.
    - src/ai/master/vault/projector.ts (lines 661-676 for the `EncounterState` shape: `monsters[].name` / `.id` / `.ac?` optional / `.hpCurrent` / `.isAlive`; and lines 780-789 for the `monster_hp_change` reducer clamp `Math.max(0, hpCurrent + delta)` + unknown-id defensive skip — MIRROR the rule in tests, do NOT re-implement persistence)
    - src/ai/master/vault/events-schema.ts (lines 321-322 for the exact `turn_advance` payload `Record<string, never>` and `monster_hp_change` payload `{ id: string; delta: number }`; the resolver constructs these as plain `{type,payload}` — NO envelope)
    - src/lib/roll-parser.ts (`extractPurpose` ~line 744 — the `per`/`for` lead-in gate that FORCES the damage-request `per` form; `inferKind` ~line 804 — `danni`→damage, `attacc`→attack)
    - src/components/game/roll-request-button.tsx (lines 125-134 `formatResultText` — the EXACT roll-result string the resolver parses: `🎲 I rolled **<total>** for <label> (<dice><mod>).`; line 126 `showBreakdown=false` when `rolls.length<=1 && modifier===0` → the `+0` no-breakdown case)
    - src/engine/combat/attack.ts (lines 345/361/365 — the hit rule to MIRROR, not call: `:345` nat-1 miss, `:361` nat-20 crit, `:365` `hit = naturalCrit || total >= effectiveAc`)
    - 08-RESEARCH.md § "Code Examples" — the EXECUTION-VERIFIED parse function (LAST-parenthetical breakdown regex + `+0` fallback `natural=total`) and the hit-rule snippet; use these verbatim, do NOT re-derive (Pitfall 2)
  </read_first>
  <action>
    Create `src/app/api/sessions/[id]/turn/combat-resolver.ts` as a PURE function (no I/O, no
    clock, no env, no randomness — copy the determinism-contract JSDoc header from combat-handoff.ts
    and add a threat-model note covering: match-by-NAME→server-side-id only, never throws→null,
    HP clamp lives in the reducer).

    Export `interface ResolveCombatResult` per D-02 EXACTLY:
    `kind: 'to-hit' | 'damage' | 'none'`; `events: VaultEvent[]` (plain `{type,payload}`, import
    type from `@/ai/master/vault/events-schema`); `narrationDirective: string`; `damageRequest:
    string | null`. Export `function resolveCombat(input: { rollResult: string; encounter:
    EncounterState; defaultMonsterAc?: number; defaultDamageDie?: string }): ResolveCombatResult | null`
    (import `EncounterState` type from `@/ai/master/vault/projector`). D-08 defaults: `defaultMonsterAc`
    = 12, `defaultDamageDie` = "1d6".

    Parsing helper (use the verified snippet from 08-RESEARCH § Code Examples): extract `total` from
    `/\*\*\s*(\d+)\s*\*\*/`; dice kind from `/\bfor\s+((?:\d+)?d\d+)/i`; breakdown from the LAST
    parenthetical `/\(([\d+\-\s]+)\)\s*\.?\s*$/` over the trimmed string → `natural` = first number
    of that group; when NO trailing breakdown (the `+0` single-die case) → `natural = total`; derive
    `bonus = total - natural`. Return `null` if no `**N**`.

    Kind detection (require BOTH dice + keyword per D-03/D-04, RESEARCH Open Q2): `1d20` dice +
    label keyword `attacc`/`colp` → to-hit; non-`d20` dice + keyword `danni` → damage; anything else
    (e.g. a bare 1d20 with no attack keyword during combat) → `null`.

    Target parse + match: to-hit target = name after `attaccare`/`colpire`; damage target via
    `/danni\s+a\s+([^.;:!?\n)]+)/i`. Match CASE-INSENSITIVE EXACT against `encounter.monsters[].name`
    (RESEARCH Open Q1): `monsters.filter(m => m.name.toLowerCase() === target.toLowerCase())` — 0 or
    >1 matches → return `null` (ambiguous/unknown). Resolve the server-side `id` + `ac` from the
    single match (the player NEVER supplies an id — threat mitigation).

    Hit rule (MIRROR attack.ts:345/361/365 on the ROLLED total — do NOT import/call `makeAttack`,
    it re-rolls the d20 and needs a full Character per D-09): `const ac = monster.ac ??
    input.defaultMonsterAc ?? 12; const hit = natural !== 1 && (natural === 20 || total >= ac);`

    To-hit result: MISS → `{ kind:'to-hit', events:[{type:'turn_advance',payload:{}}],
    narrationDirective:"[RESOLVED BY SYSTEM: ...mancato...] narra in 2ª persona", damageRequest:null }`.
    HIT → `{ kind:'to-hit', events:[] (turn does NOT advance yet), narrationDirective:"[RESOLVED BY
    SYSTEM: ...colpito...] narra in 2ª persona", damageRequest: `Tira ${die}+${bonus} per danni a
    ${monster.name}` }`. The `per` lead-in is BLOCKING (RESEARCH Pitfall 1 — without it the client
    parser drops the target and the stateless two-step breaks); `die` = `input.defaultDamageDie ??
    "1d6"`; `bonus` reused from the to-hit roll.

    Damage result: `{ kind:'damage', events:[{type:'monster_hp_change',payload:{id:monster.id,
    delta:-total}},{type:'turn_advance',payload:{}}], narrationDirective:"[RESOLVED BY SYSTEM:
    ${monster.name} subisce ${total} danni] narra in 2ª persona", damageRequest:null }`.

    Edge handling (D-05/D-10 — NEVER throw, always `null` on fall-through): unparseable roll, no
    target match, ambiguous (>1) target, wrong dice+keyword combo, or not a combat roll → `return
    null`. Narration-directive exact wording is Claude's discretion (semantics LOCKED: hit/miss/-HP,
    2nd person, the `[RESOLVED BY SYSTEM: ...]` prefix form).
  </action>
  <verify>
    <automated>pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `combat-resolver.ts` exports `function resolveCombat` and `interface ResolveCombatResult`
    - `pnpm tsc --noEmit` exits 0
    - The file contains NO `Date.now`, `Math.random`, `randomUUID`, `process.env`, or filesystem reads (pure function — `grep -E 'Date\.now|Math\.random|randomUUID|process\.env|readFile|parseEventsFile' 'src/app/api/sessions/[id]/turn/combat-resolver.ts'` returns no matches)
    - The damage-request literal uses the `per danni a` lead-in (`grep -F 'per danni a' 'src/app/api/sessions/[id]/turn/combat-resolver.ts'` matches)
    - `resolveCombat` does NOT import or call `makeAttack` / `applyDamage` (`grep -E 'makeAttack|applyDamage' 'src/app/api/sessions/[id]/turn/combat-resolver.ts'` returns no matches)
  </acceptance_criteria>
  <done>resolveCombat + ResolveCombatResult exported as a pure function; tsc clean; per-form damage request literal present; no engine-attack import.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Headless REQ-039 unit suite for resolveCombat</name>
  <files>tests/app/api/sessions/[id]/turn/combat-resolver.test.ts</files>
  <read_first>
    - tests/sessions/vault-combat-turn-interleaving.test.ts (lines 1-2 imports; lines 59-93 the `EncounterState` fixtures — copy this shape EXACTLY: pure-fn test, NO db mock, NO tmpfs, NO scripted provider, just `import {...}; const FIXTURE: EncounterState = {...}; expect(fn(args)).toEqual(...)`)
    - src/app/api/sessions/[id]/turn/combat-resolver.ts — the function under test (its exact return contract from Task 1)
    - src/lib/roll-parser.ts (`parseRollRequests` export — used to assert the `damageRequest` round-trips with the target name)
    - 08-RESEARCH.md § "Code Examples" — the VERIFIED roll-result fixture strings (`🎲 I rolled **18** for 1d20+3 (attaccare Veyra) (15+3).`, the `+0` no-breakdown form `🎲 I rolled **18** for 1d20 (attaccare Veyra).`, nat-20 `(20+3)`)
    - 08-VALIDATION.md § "Per-Task Verification Map" (the Wave 0 unit rows — every assertion below maps to a row)
  </read_first>
  <action>
    Create `tests/app/api/sessions/[id]/turn/combat-resolver.test.ts`. This is a NEW file that
    also creates the directory `tests/app/api/sessions/[id]/turn/` (it does not exist yet). Import
    `{ describe, it, expect } from 'vitest'`, `{ resolveCombat } from '@/app/api/sessions/[id]/turn/combat-resolver'`,
    `type { EncounterState } from '@/ai/master/vault/projector'`, and `{ parseRollRequests } from
    '@/lib/roll-parser'`.

    Define `EncounterState` fixtures copied from the turn-interleaving shape: one ACTIVE encounter
    with a monster that HAS `ac` (e.g. Veyra ac 14) AND a monster WITHOUT `ac` (to assert the
    default-12 path). Use the VERIFIED roll-result fixture strings from 08-RESEARCH § Code Examples.

    Cover every REQ-039 Wave 0 row from 08-VALIDATION.md (use exact describe/it titles so
    `-t "to-hit hit"` filters work):
    - to-hit total >= AC → `result.kind==='to-hit'`, `events` empty, `damageRequest` matches `/per danni a/` (HIT case)
    - to-hit total < AC → `kind==='to-hit'`, `events` has one `turn_advance`, `damageRequest` null (MISS)
    - nat 20 below AC → still HIT (assert `damageRequest` non-null even when `total < ac`)
    - nat 1 at/above AC → still MISS (assert `damageRequest` null even when `total >= ac`)
    - `+0`/no-breakdown roll (`1d20 (attaccare Veyra).`) → parses with `natural=total`, nat-20-on-+0 auto-hits
    - damage roll → `events` = `[monster_hp_change{id,delta:-total}, turn_advance]` (assert delta sign + target id)
    - target parsed case-insensitive from `danni a <name>` (mixed-case fixture matches the monster)
    - unknown target → `null`; ambiguous (>1 exact-name match, two monsters same name) → `null`
    - default AC 12 when `monster.ac` absent (a total of 12 hits the no-ac monster; 11 misses)
    - default die `1d6` appears in the damage request (`/1d6\+/` matches `damageRequest`)
    - `1d20` during combat with NO attack keyword → `null`; non-d20 with no `danni` keyword → `null`
    - garbage string (e.g. `"hello there"`) → `null` and does NOT throw (`expect(() => resolveCombat(...)).not.toThrow()`)
    - ROUND-TRIP: take the HIT-case `result.damageRequest`, run `parseRollRequests(result.damageRequest!)`,
      assert the parsed request carries the target name (the `per danni a` form round-trips — RESEARCH Pitfall 1)
  </action>
  <verify>
    <automated>pnpm vitest run tests/app/api/sessions/\[id\]/turn/combat-resolver.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run tests/app/api/sessions/\[id\]/turn/combat-resolver.test.ts` exits 0
    - A to-hit total >= AC returns `kind:'to-hit'`, empty `events`, and `damageRequest` matching `/per danni a/`
    - A to-hit total < AC returns a single `turn_advance` event and `damageRequest === null`
    - A nat-20 below AC still returns a non-null `damageRequest` (HIT); a nat-1 at/above AC returns `damageRequest === null` (MISS)
    - A damage roll returns `events` whose first element is `monster_hp_change` with a NEGATIVE `delta` equal to `-total`, followed by `turn_advance`
    - An unknown or >1-match target returns `null`; a garbage string returns `null` without throwing
    - The default-AC-12 path is asserted on a monster with no `ac`; the default die `1d6` appears in the damage request
    - `parseRollRequests(result.damageRequest!)` on the HIT case yields a request carrying the target name
  </acceptance_criteria>
  <done>The full REQ-039 resolver-math suite is green; the per-form damage request is asserted to round-trip via parseRollRequests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → resolver | The player-controlled roll-result STRING crosses into `resolveCombat` as the turn message. Untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | `resolveCombat` target match | mitigate | Match by NAME (case-insensitive EXACT) against `encounter.monsters[].name`, then resolve the server-side `id`. The player can NAME a monster but cannot inject an arbitrary `id`; unknown/ambiguous name → `null`. Reducer also defensively skips unknown ids (`projector.ts:784`). |
| T-08-02 | Tampering / DoS | damage total → HP | mitigate | The resolver only emits the `delta`; the `monster_hp_change` reducer clamps `Math.max(0, hp+delta)` (`projector.ts:786`) — no negative HP, no overflow. A hostile huge total bottoms HP at 0. (HP-clamp itself is the reducer's job — this plan mirrors the rule in tests only.) |
| T-08-03 | DoS | crafted string → exception → 500 | mitigate | Contract: `resolveCombat` NEVER throws → returns `null` on any unparseable/ambiguous input (D-05/D-10). Verified by the "garbage string → null, no throw" unit test (Task 2). |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | This phase installs NO external packages (RESEARCH § Package Legitimacy Audit — pure TS reusing internal modules; vitest already present). No install task; no slopcheck needed. |
</threat_model>

<verification>
- `pnpm vitest run tests/app/api/sessions/\[id\]/turn/combat-resolver.test.ts` green (all REQ-039 resolver-math rows).
- `pnpm tsc --noEmit` clean.
- `combat-resolver.ts` is pure (no Date.now / Math.random / randomUUID / process.env / fs reads).
</verification>

<success_criteria>
- `resolveCombat` deterministically returns the D-02 contract for to-hit (hit/miss vs AC), damage (HP delta + advance), and `null` on every fall-through edge — verified headless.
- Nat-20 auto-hits, nat-1 auto-misses, default AC 12, default die 1d6, case-insensitive exact target match, ambiguous/unknown → null — all covered by green tests.
- The emitted damage-request uses the `per danni a <target>` form and round-trips through `parseRollRequests` carrying the target (RESEARCH Pitfall 1 closed).
</success_criteria>

<output>
Create `.planning/phases/08-server-side-combat-resolver-v1-player-attacks/08-01-SUMMARY.md` when done.
</output>
